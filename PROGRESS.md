# Progress Log

## Foundation

- [x] Establish coursework folder architecture per CLAUDE.md governance
  - Date: 2026-07-30
  - Session: CC-20260730-1mk2
  - What changed: Created `directives/` (with `week-03.md` template), `src/week-03/`, `tests/week-03/`, `docs/week-03/`, each with a short README; added `docs/ARCHITECTURE.md` documenting the approved structure, scope decision, and traceability. No existing files (`README.md`, `src/memory-match.html`, `.claude/agents/`) were moved or modified.
  - Verification: user confirmed ("APPROVE FOUNDATION")
  - Notes: Scope decision — this repo is treated as a personal coursework workspace, not the Colaberry production system CLAUDE.md's backend/frontend sections describe (user-confirmed). `scripts/` intentionally deferred: CLAUDE.md's HTML changelog rule assumes a Node toolchain this repo doesn't have, and this change's approval explicitly excluded installing dependencies — see `docs/ARCHITECTURE.md` for full reasoning. Placement of pre-existing `src/memory-match.html` into a `week-XX/` folder is an open question, left untouched pending user decision.

- [x] Track pre-existing `src/memory-match.html` in git
  - Date: 2026-07-30
  - Session: CC-20260730-1mk2
  - What changed: Added `src/memory-match.html` to git (it existed on disk but was untracked). No content changed, no move into a `week-XX/` folder.
  - Verification: user confirmed ("track the memory-match.html")
  - Notes: Which week folder this belongs in is still an open question — file left at `src/memory-match.html`, not relocated.

## Week 3

- [x] Implement `add(a, b)` as a Plan Mode worked example
  - Date: 2026-07-30
  - Session: CC-20260730-1mk2
  - What changed: Filled in `directives/week-03.md` with the add-function brief; added `src/week-03/add.js` (`add(a, b) { return a + b; }`); added `tests/week-03/add.test.md` checklist (happy path + negative + zero cases).
  - Verification: Node.js is not installed in this environment (`node` not found via Bash or PowerShell), so the checklist was verified by manual code trace, not execution — `add(2,3)`→5, `add(-1,1)`→0, `add(0,0)`→0 all follow directly from `return a + b`. Recommend running the command in `tests/week-03/add.test.md` yourself once Node is available, for an actually-executed confirmation.
  - Notes: This task was used as a deliberately trivial Plan Mode demonstration — Claude Code's own planning guidance flags "single function, clear requirements" as a case that doesn't need formal planning; plan mode was used here anyway per explicit user request, for teaching purposes.

## Deep Dives

- [x] Build Business Analyst Field Guide (Week 1 deep dive) as a self-contained HTML knowledge base
  - Date: 2026-07-30
  - Session: CC-20260730-1mk2
  - What changed: Added `docs/BusinessAnalysis_FieldGuide.html` (~1,830 lines, single self-contained file) — a knowledge-base-style guide with left topic nav, search, and an offline Ask-the-Guide FAQ assistant (21 entries, no external API). Fetched the official Colaberry logo from `https://enterprise.colaberry.ai/colaberry-logo-transparent.png` and embedded it as an inline base64 data URI throughout (header + every document cover). Used only the mandated exact hex colors (Cherry Red #FB2832, Berry Blue #367895, Leaf Green #5BA63C, Amber #E8920C, specified neutrals and dark-mode variants) and Roboto/Roboto Mono typography. Built a Restaurant-industry worked example (Harvest & Hearth Restaurant Group, "SmartServe AI" demand-forecasting initiative) with 9 complete documents: BRD (34 requirements, 20 FR + 14 NFR, MoSCoW-prioritized 16/12/4/2), Vision & Business Case (3-year ROI table, ~21-month payback), User Stories (12 stories with Given/When/Then), Use Cases (3, each with alternate + exception/unhappy paths), Personas (4, goals/frustrations format), Stakeholder Matrix (12 stakeholders + RACI), Current/Future-State Process (8-step flows each), Requirements Traceability Matrix (all 34 requirements traced to a business objective and test case, or explicitly marked Deferred — no silent orphans), and a one-page Executive Summary. Each document renders as a styled deliverable (cover, doc-control strip, sign-off block, footer) with per-document "Download HTML", "Save as PDF" (print-triggered popup of the same styled document), and (for the 3 tabular documents) "Download CSV" buttons, all backed by inline JS with no external libraries. Visuals are hand-built inline SVG (no CDN): a MoSCoW bar chart, a story-priority bar chart, a stakeholder power/interest scatter, an 8-week MAPE trend line chart, an order-mix donut chart, and two 8-step process-flow diagrams. Embedded `<script type="application/json" id="deepdive-metadata">` with the required fields. Opened the file in the default browser to confirm it renders.
  - Verification: JSON metadata block validated with a Python `json.loads` parse (passed); HTML tag-balance sanity check (div/section/table/script/style open vs. close counts all matched); file opened successfully via `Start-Process` in the default browser. Manual click-through of search, Ask assistant, and download/print/CSV buttons was **not** performed in this session — recommend the user exercise those interactive paths directly since browser-side JS execution isn't independently verifiable from this environment.
  - Notes: Node.js is unavailable in this environment, so no JS linter/test harness ran against the embedded script — correctness rests on careful authoring plus the static checks above, not automated execution. `student_id` in the metadata block was set to `regina.asafor` (derived from the known user identity) since no course-issued student ID was provided; flagging this as an assumption. File placed at `docs/BusinessAnalysis_FieldGuide.html` per this repo's existing "docs/ = in-repo documentation" convention (see `docs/ARCHITECTURE.md`) — no new folder was created for this single-file deliverable.

## Agent Skills Lab

- [x] Build `data-quality-gate` Agent Skill and sample lab dataset
  - Date: 2026-08-03
  - Session: CC-20260803-k9pz
  - What changed: Created `.claude/skills/` (did not previously exist; only `.claude/agents/` was present) and added `.claude/skills/data-quality-gate/SKILL.md` — a procedural, read-only data-quality-gate skill (schema, freshness, volume, key uniqueness, duplicates, required fields, nulls, numeric rules; outputs a Check/Evidence/Status/Recommended Action table, then PASS/WARN/FAIL and PUBLISH/BLOCK). No `allowed-tools` field yet, per instructions. Added `skill-lab/orders.csv` (12 order records) with four deliberate defects for the skill to catch: duplicate `order_id` (`ORD-1004` appears twice), one blank `region` (`ORD-1005`), one negative `revenue` (`ORD-1008`, -100.00), and one stale `load_timestamp` (`ORD-1010`, dated 2026-07-30, >48h before the 2026-08-03 file date). Added `skill-lab/quality-contract.md` defining the five contract rules the skill checks against (`order_id` unique, `region` required, `revenue` > 0, `load_timestamp` < 24h old, row count ≥ 10).
  - Verification: Files created and read back to confirm content; row count in `orders.csv` counted (12 rows) and cross-checked against the four required defects, each isolated to a single row for clarity. Skill was **not** run/invoked in this session, per explicit user instruction ("do not run the Skill yet") — no PASS/WARN/FAIL output has been produced or verified yet.
  - Notes: Nothing was committed to git, per explicit user instruction. No conflict with this repo's established scope (see the Foundation entry above — this repo is a personal coursework workspace, not the Colaberry production system CLAUDE.md's backend/frontend sections describe), so no escalation was needed. New top-level `.claude/skills/` directory was created after this session's Claude Code process started — see session summary for the restart recommendation.

- [x] Run `data-quality-gate` skill against `skill-lab/orders.csv`
  - Date: 2026-08-03
  - Session: CC-20260803-k9pz
  - What changed: Invoked the `data-quality-gate` skill (it was discoverable this session without a restart — the earlier restart caveat did not end up applying) against `skill-lab/orders.csv` using `skill-lab/quality-contract.md` as the contract. Ran all 8 checks (schema, freshness, expected volume, key uniqueness, duplicates, required fields, nulls, numeric rules) and wrote the results to a new file, `skill-lab/data-quality-report.md`. `orders.csv` was read-only throughout; no source data was modified.
  - Verification: Report reviewed against the source CSV line-by-line: 4 checks FAILED (freshness — `ORD-1010` load_timestamp 2026-07-30, >24h old; key uniqueness — `ORD-1004` duplicated on CSV lines 5 and 11; required fields — `ORD-1005` blank `region`; numeric rules — `ORD-1008` revenue -100.00), 4 checks PASSED (schema, expected volume, full-row duplicates, other nulls). Overall result **FAIL**, recommendation **BLOCK**.
  - Notes: Nothing committed to git, per explicit user instruction. This was a genuine skill invocation (via the Skill tool, triggered by the request matching the skill's stated purpose), not a manual re-implementation of its logic.

- [x] Harden `data-quality-gate` trigger specificity and split reference docs
  - Date: 2026-08-03
  - Session: CC-20260803-k9pz
  - What changed: Rewrote the `description` in `.claude/skills/data-quality-gate/SKILL.md` to explicitly list positive triggers (dataset/CSV/ETL-output/query-result validation, publish-readiness questions) and explicit negative triggers (ordinary SQL writing, metric calculation, dashboard design/build alone do not invoke it). Added a "When NOT to use" section with guidance for mixed requests (do the SQL/metric/design work directly; only a separate publish-readiness ask invokes the skill). Moved the detailed per-check definitions (what each of the 8 checks verifies, evidence expectations, PASS/WARN/FAIL criteria, and no-contract defaults) out of SKILL.md into a new reference file, `.claude/skills/data-quality-gate/references/quality-checks.md`; SKILL.md now just lists the 8 check names and a one-line instruction to read the reference before scoring. Added `skill-lab/data-quality-gate-tests.md` with 3 prompts expected to trigger the skill, 3 expected not to trigger it, and the expected-output requirements for each side.
  - Verification: SKILL.md re-read after edit to confirm the "When NOT to use" section and the reference-file pointer are present; `references/quality-checks.md` re-read to confirm all 8 checks from the original SKILL.md were preserved (none dropped) with expanded criteria; `skill-lab/data-quality-gate-tests.md` re-read to confirm 3 positive / 3 negative prompts and output requirements are present. Not re-run end-to-end against `orders.csv` in this session (no source-data change occurred, so the prior run's PASS/WARN/FAIL result is still valid).
  - Notes: Nothing committed to git, per explicit user instruction. Existing skill was edited in place per the "do not rebuild from scratch" instruction — the check list, output table format, and PUBLISH/BLOCK logic are unchanged from the original.

- [x] Build `etl-failure-triage` Agent Skill and sample failure scenario
  - Date: 2026-08-03
  - Session: CC-20260803-k9pz
  - What changed: Confirmed `.claude/skills/` already existed (from the earlier `data-quality-gate` work this session) — not recreated. Added `.claude/skills/etl-failure-triage/SKILL.md` — a procedural, evidence-first triage skill (facts vs. hypotheses, ranked causes each with cited evidence, one safe read-only next diagnostic step per cause, no code changes, no reruns) returning Incident Summary / Evidence / Ranked Causes / Next Tests / Escalation Recommendation. Added `.claude/skills/etl-failure-triage/references/common-failures.md` cataloging 8 common ETL/ELT failure signatures (schema drift, failed mapping, retry exhaustion, timeout, upstream unavailability, partial load, credential/auth failure, downstream constraint violation) with evidence requirements and typical next steps for each. Added `skill-lab/orders-pipeline-failure.log` — a synthetic `orders_pipeline` job log (2 attempts) showing a `region` schema mismatch (VARCHAR free-text vs. expected ENUM), a failing `region_code_map` mapping step (322/1482 then 326/1486 rows unresolved), and a retry that reproduced the identical failure before retries were exhausted. Added `skill-lab/pipeline-run-metadata.md` with matching run metadata (attempt history table, last known-good run, and a recorded 2026-08-02 source schema change to `raw_orders.region` with no corresponding mapping-table update) — the causal thread a later triage run would need to cite as evidence.
  - Verification: All four files read back after creation to confirm content and cross-file consistency (run_id `run-20260803-2210`, row counts, and timestamps match between the log and the metadata file). Skill was **not** invoked in this session, per explicit user instruction ("Do not commit or run the Skill").
  - Notes: Nothing committed to git, per explicit user instruction. User referred to this as "the Week 2 Agent Skills lab"; no `directives/week-02.md` exists in this repo (only `week-03.md`) — treated as the user's own label for this lab exercise, not a conflict requiring escalation.

- [x] Run `etl-failure-triage` skill against the orders pipeline failure scenario
  - Date: 2026-08-03
  - Session: CC-20260803-k9pz
  - What changed: Invoked the `etl-failure-triage` skill against `skill-lab/orders-pipeline-failure.log` and `skill-lab/pipeline-run-metadata.md`. Wrote the 5-section triage output (Incident Summary, Evidence, Ranked Causes, Next Tests, Escalation Recommendation) to a new file, `skill-lab/etl-triage-report.md`. Both source files were read-only throughout; no pipeline code was touched and no job was rerun.
  - Verification: Report reviewed against the two source files line-by-line — every cited log line, row count, and timestamp in the report traces back to the log or metadata file. Ranked causes: (1) source schema drift on `region` (VARCHAR widened to free-text on 2026-08-02), (2) `region_code_map` mapping table stale since 2026-06-14, (3) retry reproduced the identical failure, confirming the cause is deterministic rather than transient. Escalation recommendation: escalate now to data-engineering/source-system owner, on-call already paged via `data-eng-alerts`.
  - Notes: Nothing committed to git, per explicit user instruction. No pipeline changes or reruns performed — consistent with the skill's constraints and the user's explicit instruction.

- [x] Run the full publish-readiness incident workflow and assemble the final incident package
  - Date: 2026-08-03
  - Session: CC-20260803-k9pz
  - What changed: Ran the two-skill incident workflow end to end ahead of the scheduled executive dashboard publish. (1) Invoked `data-quality-gate` on `skill-lab/orders.csv` against `skill-lab/quality-contract.md` — result FAIL/BLOCK (same 4 failing checks as the earlier run: freshness, key uniqueness, required fields, numeric rules). (2) Because the data was unsafe, invoked `etl-failure-triage` on `skill-lab/orders-pipeline-failure.log` and `skill-lab/pipeline-run-metadata.md` — same ranked causes as the earlier run (source schema drift on `region`, stale `region_code_map`, retry confirming a non-transient cause). (3) Synthesized a concise executive brief from both reports' findings only — explicitly left owner, resolution time, and financial impact as "not yet known" rather than inventing values, per instruction. Saved all three to a new `skill-lab/final-incident-package/` folder: `data-quality-report.md`, `etl-triage-report.md`, `executive-dashboard-brief.md`. Also built `skill-lab/final-incident-package/executive-dashboard.html` — a self-contained, light/dark-aware incident-status page (BLOCK banner, stat tiles, checks table, ranked-causes list, run timeline, "not yet known" panel, next-action callout) using the dataviz skill's status-color and chrome tokens — and opened it in the default browser.
  - Verification: All three markdown reports re-derived from the source CSV/log/metadata (not copied blind) and cross-checked against the earlier same-day reports for consistency — no drift, since none of the underlying source files changed. HTML page opened via `start` in the default browser; every stat/status shown on the page traces to a line in `data-quality-report.md` or `etl-triage-report.md`, no invented owner/ETA/financial figures anywhere on the page.
  - Notes: Nothing committed to git, per explicit user instruction. Source data (`orders.csv`) and pipeline files were not modified; no job was rerun. `orders.csv` and `quality-contract.md` (unchanged) confirmed identical to the last read via the Read tool's "file unchanged" signal before re-validating.

## Deep Dives

- [x] Build Solution Architect Field Guide (Week 2 deep dive) as a self-contained HTML knowledge base
  - Date: 2026-08-05
  - Session: CC-20260803-k9pz
  - What changed: Added `docs/SolutionArchitecture_FieldGuide.html` (~1,875 lines, single self-contained file) — modeled on the existing `docs/BusinessAnalysis_FieldGuide.html` scaffold (same exact Colaberry brand tokens, header/search/left-nav/Ask-assistant/theme-toggle pattern, per-document Download HTML / Save-as-PDF / Download CSV pattern), reusing that file's already-fetched Colaberry logo base64 data URI rather than re-fetching it (identical bytes, required identical across documents). Built a P&C insurance worked example (Meridian Mutual Insurance, "ClaimsPilot AI" AI-assisted FNOL intake/triage/straight-through-processing initiative) with a concise 8-subtopic solution-architecture discipline primer (quality attributes, architecture styles, C4 model, ADRs, trade-off analysis, key patterns, fitness functions, build-vs-buy) and 8 complete deliverables: Solution Architecture Document, C4 Diagrams (Context/Container/Component), 6 Architecture Decision Records (each with rejected options and consequences), a 16-row Non-Functional Requirements Spec with measurable targets, a Technology Trade-off Matrix (4 weighted comparisons stating costs alongside wins), Sequence & Data-Flow Diagrams (2 sequence flows + a PII-boundary data-flow diagram), a 10-row Architecture Risk Register, and a one-page Architecture Package Summary with KPI tiles and a conditional pilot-approval recommendation. All diagrams (C4 x3, sequence x2, data-flow) and charts (bar, donut, line) are hand-built inline SVG via generic reusable JS renderers (`boxDiagram`, `sequenceDiagram`, `makeBarChart`, `makeDonut`, `makeLineChart`) — no external charting/diagramming libraries. Embedded the required `deepdive-metadata` JSON script tag (week 2, discipline "Solution Architect"). Opened the file in the default browser to confirm it renders.
  - Verification: Automated post-write checks (not just visual): div/section/table/script/style tag-open vs. tag-close counts all matched (375/17/13/2/2), no duplicate HTML element IDs across the file, and the `deepdive-metadata` JSON block parsed successfully via Python `json.loads`. File opened successfully via `start` in the default browser. Manual click-through of search, the Ask assistant, per-document download/print/CSV buttons, and dark-mode toggle was **not** performed in this session — recommend the user exercise those interactive paths directly since browser-side JS execution isn't independently verifiable from this environment.
  - Notes: All figures (weighted trade-off scores, NFR targets, cost-per-claim breakdown, risk register) are invented as part of the fictional worked example, per the task's explicit instruction to "invent a believable initiative" — this differs from the unrelated ETL-incident-brief task earlier in this session, where inventing financial impact/owner/ETA was explicitly disallowed because that was a real (if lab-simulated) incident, not a fictional teaching example. `student_id` in the metadata block set to `regina.asafor`, consistent with the prior deep dive's convention. File placed at `docs/SolutionArchitecture_FieldGuide.html` per this repo's existing "docs/ = in-repo documentation" convention.

## Prompt Library

- [x] Scaffold the reusable prompt library structure
  - Date: 2026-08-24
  - Session: CC-20260824-r7x2
  - What changed: Created `prompts/CONTRIBUTING.md` (one-line stub) and `prompts/_template/` containing `v1.0.0.md` (blank front-matter template with fields name/version/purpose/model/inputs/output/last_eval/status, plus empty Instructions/Input/Output Fields sections) and `eval.jsonl` (one example line showing the input/expected_output/notes format). No actual prompts were written, per explicit instruction.
  - Verification: Files created and contents confirmed via the Write tool; no existing files were touched or overwritten.
  - Notes: Root already contained a `.env` with a real `ANTHROPIC_API_KEY` value and a `.gitignore` already covering `.env` — confirmed via `git log --all -- .env` that the key was never committed. Did not create or overwrite `.env` to avoid destroying the existing key; paused and asked the user how to proceed. User chose to keep the real key and add the requested placeholder as a commented reference line instead — appended `# ANTHROPIC_API_KEY=paste-your-key-here` (commented, non-functional) to the end of the existing `.env`. Also flagged that the file's existing `# Example:` comment sits directly above the real key rather than an example, which could mislead a future reader; left unchanged pending user decision since it wasn't part of the request.

- [x] Build the first 5 prompts for the incident-investigation prompt library
  - Date: 2026-08-24
  - Session: CC-20260824-r7x2
  - What changed: Selected and built 5 reusable, structured-output prompts scoped to the data-engineering incident-investigation project, following the existing `prompts/_template/v1.0.0.md` convention (each gets its own folder with `v1.0.0.md` + `eval.jsonl`): `prompts/classify-log-error/` (raw log → error_category/severity/confidence), `prompts/score-data-quality-check/` (check numbers → PASS/WARN/FAIL verdict + score), `prompts/map-dependency-impact/` (failed node + dependency edges → ranked downstream blast radius), `prompts/rank-root-causes/` (combined findings from the first three → ranked candidate causes with confidence), `prompts/recommend-corrective-action/` (top cause → one recommended action, risk level, human-approval flag). Each `v1.0.0.md` has concrete Instructions (fixed enums, explicit thresholds/roll-up rules, JSON-only output contract) and Output Fields; each `eval.jsonl` has one worked example with a realistic input/expected_output pair and a notes field explaining a non-obvious scoring/ranking decision. Chosen build order documented for the user beforehand: classify-log-error and score-data-quality-check first (highest run-frequency, foundational), then map-dependency-impact, then rank-root-causes and recommend-corrective-action (both depend on the first three's structured outputs).
  - Verification: All 10 files read back after writing to confirm frontmatter fields, JSON-only instruction, and enum/threshold completeness; each `eval.jsonl` example manually traced against its own prompt's stated rules (e.g. score-data-quality-check's 100-30(FAIL)-10(WARN) roll-up math checked by hand against the example's one FAIL + one WARN → score 60). Prompts were not run against a live model in this session — no `last_eval` evidence exists yet, so `status: draft` and `last_eval: not yet run` were left honest in every frontmatter block rather than marked complete.
  - Notes: This continues the same day's "Scaffold the reusable prompt library structure" entry above under the same session ID. Nothing was committed to git in this step, consistent with that entry's pattern of not committing prompt-library work without explicit instruction — the user has not yet asked for a commit this session.

- [x] Manually test the 5-agent incident-investigation prompt chain against `data_engineering_incident_test.json`
  - Date: 2026-08-24
  - Session: CC-20260824-r7x2
  - What changed: No files changed. Ran the full 5-prompt chain (classify-log-error → score-data-quality-check → map-dependency-impact → rank-root-causes → recommend-corrective-action) by manually following each prompt's written Instructions against the incident file's fields (`execution_logs`, `data_quality_checks`, `pipeline_metadata`/dependency edges), feeding each step's output into the next per each prompt's declared input contract.
  - Verification: Final chain output (top cause: upstream `customer_type`→`customer_category` rename not reflected in the silver transformation SQL; recommended action: patch_code, validate downstream, rerun, add schema-change validation) matched the file's embedded `expected_root_cause_for_testing` answer key closely. User was offered the option to instead build a real API-based blind-test script (reusing the `anthropic` package pattern already present in `business-workflow-assistant.py`) and chose the manual walkthrough as sufficient for now.
  - Notes: This was **not** a blind test — the answer key was visible in the same input file before reasoning through each step, so the match only demonstrates the 5 prompts' internal logical coherence (each correctly follows its own instructions given its inputs), not that a live model call would converge on the same answer unaided. Flagged this limitation explicitly to the user. No `last_eval` fields in the prompt templates were updated, since no live model run occurred — left as `not yet run` per the existing honesty convention from the prior entry.

- [x] Generate a second incident test fixture, `data_engineering_incident_test_2.json`
  - Date: 2026-08-24
  - Session: CC-20260824-r7x2
  - What changed: Added `data_engineering_incident_test_2.json` at repo root, matching the JSON shape of the existing `data_engineering_incident_test.json` (same top-level keys, same pipeline stage/layer taxonomy) so either file can feed the 5-prompt chain interchangeably. Deliberately different scenario: a nightly inventory-reconciliation pipeline OOMs mid-join (resource_limit_exceeded, not schema_mismatch), with data-quality failures on row_count + null_rate + freshness while schema_check PASSes (inverse pattern from the first file, which failed row_count + schema and passed null_rate). Added an explicit `dependency_graph.edges` + `criticality_tags` block (3 hops deep, includes nodes explicitly tagged `critical`, which the first file's graph never reached) so map-dependency-impact can be tested against both inferred and explicitly-tagged criticality. Includes two competing contributing factors in `recent_changes` (a ~40% data-volume increase from new-region onboarding, and a config change that disabled a broadcast join) so rank-root-causes has to weigh evidence rather than converge on one obvious cause. Includes the same `expected_root_cause_for_testing` answer-key convention as the first file, plus a new `test_design_notes` field documenting which code paths this fixture is meant to exercise differently from the first.
  - Verification: File written and re-read to confirm valid JSON structure and internal arithmetic consistency (e.g. `freshness_hours_actual` 51 vs `last_successful_gold_refresh` 2026-08-19T00:45 to `reported_at` 2026-08-21T03:45 = exactly 51 hours; `null_supplier_id_count` 20763 ≈ 6.2% of `gold_row_count_actual` 334892, above the 5% FAIL threshold in `score-data-quality-check`'s instructions). Not yet run through the prompt chain in this session.
  - Notes: Purely fictional data invented for testing purposes, consistent with this repo's established convention for lab/teaching fixtures (see the Deep Dives entries above). Nothing committed to git, per this session's existing pattern of not committing prompt-library/test-data work without explicit instruction.

- [x] Manually test the 5-agent chain against `data_engineering_incident_test_2.json`
  - Date: 2026-08-24
  - Session: CC-20260824-r7x2
  - What changed: No files changed. Ran the same manual walkthrough method used for the first fixture — followed each prompt's written Instructions in sequence (classify-log-error → score-data-quality-check → map-dependency-impact → rank-root-causes → recommend-corrective-action), chaining outputs per each prompt's declared input contract.
  - Verification: Category-level result matched the answer key (resource_limit_exceeded ≈ "Resource Exhaustion") and the recommended fix landed close to the answer key's fix (memory/join-strategy adjustment, clear partial partitions, rerun) purely from generic OOM reasoning. However, the answer key's *specific* causal mechanism (a config change disabled the broadcast join right as data volume grew ~40%) was NOT reproducible by the chain — confirmed by tracing each prompt's declared `inputs` field and finding that neither `classify-log-error` nor `rank-root-causes` (nor any other prompt) has `recent_changes` or `sql_info` in its input contract, so that evidence is structurally never seen by any agent in the pipeline.
  - Notes: This is a genuine design-gap finding, not a test failure on my part — surfaced via the deliberately different second fixture (the first fixture's schema-mismatch scenario didn't expose this because all needed evidence was already visible in log_text/schema_diff). Flagged two remediation options to the user (add `recent_changes` as an optional input to `rank-root-causes`, or add a new upstream prompt that surfaces deploy/config history as its own structured finding) but implemented neither — this is a design decision, not an implementation detail, so it was left for the user to choose rather than acted on unilaterally.

- [x] Close the deploy/config-history gap: add `check-recent-changes` prompt and extend `rank-root-causes` to `v1.1.0`
  - Date: 2026-08-24
  - Session: CC-20260824-r7x2
  - What changed: User chose the "new prompt" option over directly extending an existing prompt's contract, to preserve rank-root-causes' existing design rule ("reason only over structured findings, not raw data"). Added `prompts/check-recent-changes/v1.0.0.md` — a new specialist prompt that takes `incident_timestamp`, `failed_node`, optional `related_systems`, and a raw `recent_changes` list, and outputs `related_changes` (each with `hours_before_incident` and a high/medium/low `risk`), `highest_risk_change`, and `summary`. Relevance requires both a system-name match AND a 7-day lookback window — a change failing either test is excluded even if it looks suspicious. Added `prompts/check-recent-changes/eval.jsonl` with one worked example exercising both exclusion rules independently (unrelated system; related system but too old). Added `prompts/rank-root-causes/v1.1.0.md` (kept `v1.0.0.md` unmodified/immutable, added the new version as a sibling file) — extends the input contract with an *optional* `change_findings` field and instructs the coordinator to treat a `high`-risk related change as a strong contributing cause only when it's also consistent with the log/data-quality signals, not on its own. `recommend-corrective-action` was left unchanged — it consumes `rank-root-causes`' output, so a more specific `top_cause` flows through automatically without any contract change there.
  - Verification: Re-ran the manual walkthrough — fed `data_engineering_incident_test_2.json` through the new `check-recent-changes` prompt, then fed that output plus the original three findings into `rank-root-causes` v1.1.0. The resulting `top_cause` ("config change disabled the broadcast join... combined with ~40% volume increase from EMEA onboarding... exceeded executor memory") now matches the fixture's embedded `expected_root_cause_for_testing` almost exactly, closing the gap the previous entry identified. This verified worked example (not a synthetic one) was added as a second line in `prompts/rank-root-causes/eval.jsonl`. Still not run against a live model — `last_eval: not yet run` left honest in both new/updated frontmatter blocks.
  - Notes: `v1.0.0.md` of rank-root-causes is kept as an immutable historical version rather than edited in place, since the folder-per-prompt + `vX.Y.Z.md` naming convention already implies versioned files are meant to be additive, not mutated — this preserves reproducibility of any past eval run against v1.0.0. Nothing committed to git, consistent with this session's existing pattern.

- [x] Re-run `recommend-corrective-action` off the new, specific `top_cause`
  - Date: 2026-08-24
  - Session: CC-20260824-r7x2
  - What changed: No files changed (prompt itself was left as-is, per the prior entry's note that it needed no contract change). Manually re-ran recommend-corrective-action's Instructions using the fixture-2 `top_cause` produced by rank-root-causes v1.1.0 (with change_findings), plus the same supporting_evidence and impacted_nodes from earlier in the chain.
  - Verification: Output changed from the earlier generic run's `action_type: patch_code` ("increase memory and/or adjust join strategy") to `action_type: rollback` ("revert the 2026-08-20 config change that disabled the broadcast join"), plus a secondary recommendation to re-baseline memory for the EMEA volume increase. This now matches the fixture's `expected_root_cause_for_testing.recommended_fix` almost point-for-point (re-enable broadcast join, clear partitions, rerun, re-baseline resource sizing), completing the end-to-end match that the `check-recent-changes` addition was meant to enable.
  - Notes: Confirms the fix from the previous entry propagates correctly through the unmodified downstream prompt — no changes were needed to recommend-corrective-action itself, only to what fed into rank-root-causes upstream of it.

- [x] Implement add_note tool, notes://all resource, and capture_note prompt in mcp-server
  - Date: 2026-08-31
  - Session: CC-20260831-k4t9
  - What changed: Extended `mcp-server/src/server.py` (previously an empty shell per the prior "MCP server shell" commit) with a working example: `add_note(title, content)` tool (Pydantic `Field(min_length=...)` constraints for input validation), `notes://all` resource (JSON-serialized in-memory notes list), and `capture_note(raw_text)` prompt (templated user message instructing the model to derive a title/body and call `add_note`). Added `mcp-server/src/test_connection.py`, a stdio-based smoke test asserting on discovery, the tool's happy path, the tool's rejection of an empty title (isError/is_error path), the resource reflecting the tool's write, and the prompt's argument interpolation. Updated `mcp-server/README.md` to document the new tool/resource/prompt, add a "try it in the Inspector" walkthrough, and add instructions for running the automated test.
  - Verification: `python src/test_connection.py` run from `mcp-server/` → `PASS: connection, tool, resource, and prompt all verified.`, exit code 0. Also manually verified via a companion `client.js` exploration script (see Notes) that the same tool/resource/prompt design worked correctly against the Node.js SDK before porting.
  - Notes: This started as a teaching exercise built with the Node.js/TypeScript MCP SDK in the session scratchpad (not part of this repo). Before committing anything to the repo, discovered `mcp-server/` already existed with a README explicitly stating "no Node.js project... Python is what's already here." Flagged the conflict to the user via AskUserQuestion rather than silently picking a language; user chose to port the design to Python and extend the existing shell rather than introduce a Node.js toolchain. The Node.js scratch files (`server.js`, `client.js`, `test-connection.js`) were not committed to the repo and remain only in the session scratchpad.

- [x] Bring the Node.js/TypeScript MCP server reference into the repo as mcp-server/nodejs
  - Date: 2026-08-31
  - Session: CC-20260831-k4t9
  - What changed: User requested the previously-scratchpad-only Node.js implementation be committed too, for long-term reference while learning both SDKs. Copied `server.js`, `client.js`, `test-connection.js`, `package.json`, `package-lock.json` into new `mcp-server/nodejs/` folder (sibling to the Python `mcp-server/src/`), not merged into or replacing it. Renamed the package to `mcp-notes-server-nodejs` and added a header comment to `server.js` clarifying it's a side-by-side SDK-comparison reference, not a second maintained product. Added `nodejs/node_modules/` to `mcp-server/.gitignore`. Added `mcp-server/nodejs/README.md` (setup, Inspector walkthrough, `npm test`, file-by-file description) mirroring the structure of the root `mcp-server/README.md`. Updated the root README to link to `nodejs/` and clarify `src/` (Python) remains the primary version.
  - Verification: Re-ran `npm install` and `node test-connection.js` from the new `mcp-server/nodejs/` location (not just trusting the earlier scratchpad run) → `PASS: connection, tool, resource, and prompt all verified.`, exit code 0.
  - Notes: This reverses part of the "not committed" decision from the immediately prior entry — the user clarified they want both SDK versions long-term for learning purposes, not just the Python one. Kept the two implementations physically separate (`src/` vs `nodejs/`) with clear READMEs on both sides stating which one is authoritative, to avoid the two silently drifting out of sync being mistaken for a bug.

- [x] Fix mcp-server README Folders section to list nodejs/ (code review finding)
  - Date: 2026-08-31
  - Session: CC-20260831-k4t9
  - What changed: Ran `/code-review` (medium) against the `mcp-server-notes-example` PR diff. One finding: the "Folders" section at the bottom of `mcp-server/README.md` listed `src/` and `artifacts/week-05/` but never mentioned `nodejs/`, despite the top of the same file introducing it as a maintained reference implementation. Added a `nodejs/` bullet pointing to `nodejs/README.md`, and noted `src/` as the primary/maintained version in its own bullet for consistency with the rest of the file.
  - Verification: Code review re-scan of the diff surfaced no other findings; change is documentation-only (no code paths affected), reviewed by eye for correctness against the actual folder structure.
  - Notes: Docs-only fix committed onto the existing `mcp-server-notes-example` branch (PR already open) rather than a new branch.

- [x] Make the mcp-server stdio transport choice explicit in code + README
  - Date: 2026-08-31
  - Session: CC-20260831-q4h7
  - What changed: User asked to "switch the server to the transport we agreed" (stdio, per the earlier ADR-0001 discussion — everyone runs their own copy locally). Investigation found no switch was needed: `mcp-server/src/server.py` called bare `mcp.run()`, which already defaults to `transport="stdio"` in the installed SDK (`mcp` 2.1.1, confirmed from library source), and the Node.js reference already uses `StdioServerTransport` explicitly. Changed `server.py`'s entrypoint from `mcp.run()` to `mcp.run(transport="stdio")` with a comment pointing at `docs/ADR-0001-transport.md`, so the deliberate choice is visible where it takes effect rather than resting on an SDK default. Added a short "This server uses the stdio transport" paragraph to Step 2 of `mcp-server/README.md` (no network port, only reachable by a local program that launches it) linking the ADR. The README start command (`mcp dev src/server.py --with "mcp[cli]"`) was already a stdio workflow and was left unchanged — it was not inaccurate.
  - Verification: `python src/test_connection.py` from `mcp-server/` → `PASS: connection, tool, resource, and prompt all verified.`, exit code 0. The test spawns `server.py` over stdio and asserts discovery of all three primitives, `add_note` happy path, `add_note` empty-title rejection (`is_error=True`), `notes://all` reflecting the tool's write, and `capture_note` argument interpolation — i.e. tool, resource, and prompt all confirmed working over the (now explicit) stdio transport. User also confirmed manually: MCP Inspector (`mcp dev src/server.py --with "mcp[cli]"`) connects green and all three primitives show up.
  - Notes: No behaviour change — `mcp.run()` and `mcp.run(transport="stdio")` are identical at runtime; this is a self-documentation change plus a README clarification. Browser-based MCP Inspector was not driven by the agent; the automated stdio smoke test is the equivalent proof and is the CLAUDE.md-preferred form.

- [x] Add fenced file-backed persistence to the mcp-server notes store
  - Date: 2026-08-31
  - Session: CC-20260831-q4h7
  - What changed: Audited `mcp-server/src/server.py` for every filesystem / network / out-of-folder touch. Finding: there were none — the notes "store" was a module-level in-memory list, no `open()`/`pathlib`/`socket`/`subprocess`/`os.environ` anywhere; the only external channel was the stdio pipe (the transport itself). So there was no existing access to fence. Per user decision (AskUserQuestion), added the persistence layer that ADR-0001 Consequences already required, fenced from the start: new `mcp-server/src/notes_store.py` with `DATA_ROOT` = `mcp-server/data/` (resolved relative to `__file__`, not cwd; overridable via `MCP_NOTES_DATA_ROOT`), `resolve_within(root, candidate)` guard that raises `ValueError` naming the offending path for any `..` / absolute / symlink escape (never a silent empty return), `load_notes()` / `save_notes()` doing atomic writes (temp file + `os.replace`) and raising `RuntimeError` on a corrupt/unreadable existing store rather than overwriting it. `server.py` now `load_notes()` at startup and `save_notes(notes)` after each `add_note`. New `mcp-server/src/test_notes_store.py` (fence rejects `../../etc/hosts`, `../../../../etc/passwd`, `notes/../../../secret.txt`, and an absolute OS path; save/load round-trip; corrupt store raises). `test_connection.py` refactored to spawn the server with `MCP_NOTES_DATA_ROOT` set to a `TemporaryDirectory` so it no longer mutates the real store. `.gitignore` now excludes `data/`. README gained a "Where notes are stored" section + updated test/Folders sections; ADR-0001 got a dated update note under Consequences.
  - Verification: `python src/test_notes_store.py` → `ALL PASS: fence holds, persistence works.` exit 0, with each refusal printed (e.g. `refused '../../../../Users/Regina/.ssh/id_rsa' -> path escapes the notes directory: ... which is outside ...\mcp-server\data`). `python src/test_connection.py` → `PASS: connection, tool, resource, and prompt all verified.` exit 0. Manual cross-process check: added a note in one server process, process exited, `data/notes.json` on disk contained it, a fresh server process returned it from `notes://all`. `python -m py_compile` clean on all four `src/*.py`. Live out-of-fence demo run for the user showing `ValueError` on a path targeting `~/.ssh/id_rsa`.
  - Notes: The Node.js reference (`mcp-server/nodejs/`) was deliberately left in-memory — scope was the Python server ("my server"); the two now differ in persistence, which is acceptable since the READMEs already mark `src/` as authoritative and `nodejs/` as an SDK-comparison reference. No client-supplied path currently reaches `resolve_within()` (the notes filename is hardcoded); the guard is defence-in-depth plus the required call for any future path-taking tool (`import_notes(path)` etc.), which the user chose not to add now.

- [x] Mirror the fenced file-backed notes store into the Node.js reference
  - Date: 2026-08-31
  - Session: CC-20260831-q4h7
  - What changed: User asked for `mcp-server/nodejs/` to match the Python persistence work from the previous entry (reversing that entry's "left in-memory" note). New `mcp-server/nodejs/notesStore.js` mirroring `src/notes_store.py`: `DATA_ROOT` = `nodejs/data/` resolved from `import.meta.url` (not `process.cwd()`), overridable via `MCP_NOTES_DATA_ROOT`; `resolveWithin(root, candidate)` throws an `Error` naming the offending path for `..` / absolute / cross-drive / symlink escapes (uses a `realResolve()` helper that walks up to the nearest existing ancestor, `fs.realpathSync`es it, and re-appends the tail so symlinks are followed the way Python's `Path.resolve()` does); `loadNotes()` / `saveNotes()` with atomic write (temp file + `fs.renameSync`) and a thrown `Error` on a corrupt/unreadable existing store. `server.js` now `loadNotes()` at startup and `saveNotes(notes)` after each `add_note`. New `mcp-server/nodejs/test-notesStore.js` mirroring `test_notes_store.py`. `test-connection.js` now spawns the server with `MCP_NOTES_DATA_ROOT` = an `fs.mkdtempSync` dir and removes it on exit, so it never mutates the real store. `package.json`: `test` script now runs `test-connection.js && test-notesStore.js`, added `test:store`. `nodejs/README.md` gained a "Where notes are stored" section and updated test/Files sections. No new `.gitignore` rule needed — the existing `data/` rule in `mcp-server/.gitignore` already matches `nodejs/data/` at any depth (confirmed via `git check-ignore -v`).
  - Verification: `npm test` in `mcp-server/nodejs/` (Node v24.19.0) → `test-connection.js` `PASS: connection, tool, resource, and prompt all verified.` then `test-notesStore.js` `ALL PASS: fence holds, persistence works.`, exit 0, each traversal refusal printed. `node --check` clean on all four `nodejs/*.js`. Manual cross-process check: added a note via one spawned `server.js`, process exited, `nodejs/data/notes.json` on disk held it, a fresh `server.js` process returned it from `notes://all`; demo `data/` dir then deleted. Live out-of-fence demo showing `resolveWithin` throwing on a path targeting `~/.ssh/id_rsa`. Python suites (`src/test_notes_store.py`, `src/test_connection.py`) re-run after the Node changes — both still pass, exit 0. No stray `data/` directories left in the working tree.
  - Notes: `notesStore.js` uses `createdAt` (the existing Node convention) where `notes_store.py` uses `created_at`; the two stores are separate files so this is cosmetic. `realResolve()` is the one place the Node fence is more code than the Python one — Node's `path.resolve()` does not follow symlinks, so parity required walking the path manually.

- [x] Build the SQL data-source adapter as an MCP tool (run_sql_query)
  - Date: 2026-08-31
  - Session: CC-20260831-q4h7
  - What changed: Read `.colaberry/plan.json` (exists). Only external system declared is `derived.systems: ["SQL"]`; integration reqs are REQ-008 (must, "connect to data sources for log and SQL analysis"), REQ-003/STORY-003 (SQL analysis, r1), REQ-014 (should, ops notifications, r2). Recommended and built the SQL adapter — clearest value (only named system, `must`, backbone of root-cause analysis) at the least setup (SQLite via stdlib `sqlite3`, zero new deps, reuses the path fence). New `src/sql_adapter.py`: `run_sql_query(database, query, params=None, timeout_seconds=5, max_rows=100, data_root=None)`. Validates every arg before any file/DB access (`_validate` → raises `SqlAdapterError("ValidationError", …)`); fences `database` under `MCP_SQL_DATA_ROOT` (default `mcp-server/data/`); opens the connection `file:…?mode=ro` + a `set_authorizer` that hard-denies `ATTACH`/`DETACH`; probes `sqlite_master` so a missing/corrupt/locked file fails as `Unavailable` before the query runs; runs the query under a `threading.Timer` watchdog that calls `conn.interrupt()` at `timeout_seconds` (→ `SqlAdapterError("Timeout", …)`); binds `params` positionally; caps rows at `max_rows` (+`truncated`). Returns `{database, columns, rows, row_count, truncated, elapsed_ms}`. Extracted the shared path fence into new `src/fs_fence.py` (`resolve_within`, now with a `what=` label arg); `notes_store.py` re-imports/re-exports it so `from notes_store import resolve_within` still works. `src/server.py`: new `@mcp.tool() run_sql_query` wrapper — same arg contract re-declared via Pydantic `Field` (min/max length, `gt/le`, `ge/le`), and it catches `SqlAdapterError` and re-raises `mcp.server.mcpserver.exceptions.ToolError(str(exc))` so the SDK returns `is_error=True` with the tagged message instead of logging an `UnexpectedToolError` crash (this SDK, `mcp` 2.1.1, only treats `ToolError` as a graceful tool failure). New `src/test_sql_adapter.py` (10 cases). `src/test_connection.py` now also asserts `run_sql_query` is registered. README got a "The `run_sql_query` tool" section (input table + a table of every "system down" message) and updated test/Folders sections. `.colaberry/*` not touched (portal-owned).
  - Verification: `python src/test_sql_adapter.py` → `ALL PASS: sql_adapter validates, fences, times out, and fails gracefully.` exit 0 — covers validation-before-DB-access (empty/write/DDL/multi-statement/bad-timeout/bad-max_rows/bad-params), happy path with a bound `?` param, `max_rows` truncation, missing DB → `[Unavailable]`, corrupt (non-SQLite) file → `[Unavailable]`, path-fence escape → `[ValidationError] … escapes`, `ATTACH` → rejected, write via leading `WITH` → blocked by `mode=ro` → `[ValidationError] read-only`, missing table → `[QueryError]`, and a billion-row recursive CTE with `timeout_seconds=0.5` cancelled in ~0.5s (asserted < 30s, i.e. no hang). `python -m py_compile src/*.py` clean. `src/test_notes_store.py` + `src/test_connection.py` re-run after the `fs_fence` extraction — both still pass, exit 0. End-to-end over stdio (server spawned with temp `MCP_SQL_DATA_ROOT`): `run_sql_query` present in `list_tools`; UP → `is_error=False` with the JSON result; DOWN (missing file) → `is_error=True`, `Error executing tool run_sql_query: [Unavailable] database file not found: … The SQL system is unreachable.`; SLOW → `is_error=True`, `[Timeout] … cancelled after exceeding the 0.5s timeout`; WRITE → `is_error=True`, `[ValidationError] … read-only …`; server logs these as `INFO … Tool 'run_sql_query' failed`, no traceback.
  - Notes: Assumption logged and surfaced to the user — SQLite is the MVP backend for the "SQL" system; moving to Postgres/Snowflake/etc. is a driver swap inside `sql_adapter.py`, the tool contract is unchanged. Node.js reference (`nodejs/`) was NOT given a matching tool this pass — scope was "the adapter in mcp-server/" (singular); offered to mirror it on request, same as the persistence work. New dependency count: zero (stdlib `sqlite3`). Not a governance escalation — additive tool, read-only, fenced, reversible, fully tested (CLAUDE.md "adding … non-breaking interfaces" = PROCEED).

- [x] Mirror run_sql_query into the Node.js reference
  - Date: 2026-08-31
  - Session: CC-20260831-q4h7
  - What changed: User asked to mirror the SQL adapter into `mcp-server/nodejs/`. New `nodejs/sqlAdapter.js` (`runSqlQuery({database, query, params, timeoutSeconds, maxRows, dataRoot})` + `SqlAdapterError` with `errorClass`), `nodejs/sqlChild.js` (isolated child process that runs one query with the built-in `node:sqlite`, `readOnly: true` + `setAuthorizer` denying `SQLITE_ATTACH`/`SQLITE_DETACH` + `sqlite_master` probe, emits one JSON line), `nodejs/fsFence.js` (extracted `resolveWithin`/`realResolve` from `notesStore.js`, mirroring the Python `fs_fence.py` extraction; `notesStore.js` now imports + re-exports it). `nodejs/server.js`: new `run_sql_query` tool with a zod schema mirroring the Pydantic one (public arg names kept snake_case: `timeout_seconds`, `max_rows`), handler catches `SqlAdapterError` and returns `{ content:[…], isError:true }`. New `nodejs/test-sqlAdapter.js` (10 cases, mirrors `src/test_sql_adapter.py`). `nodejs/test-connection.js` now asserts `run_sql_query` is registered and passes `MCP_SQL_DATA_ROOT` (temp dir) to the spawned server. `package.json`: `test` runs `test-connection && test-notesStore && test-sqlAdapter`, added `test:sql`. `nodejs/README.md`: added "The `run_sql_query` tool" section (input list + system-down message table + the implementation-difference note) and updated the test/Files sections. Return shape kept byte-identical to Python (`{database, columns, rows, row_count, truncated, elapsed_ms}`).
  - Verification: `npm test` in `mcp-server/nodejs/` (Node v24.19.0) → all three suites pass, exit 0: `test-connection.js` `PASS: connection, tool, resource, and prompt all verified.`; `test-notesStore.js` `ALL PASS`; `test-sqlAdapter.js` `ALL PASS: sqlAdapter validates, fences, times out, and fails gracefully.` — same 10 cases as Python incl. billion-row recursive CTE with `timeoutSeconds=0.5` cancelled in ~0.53s (asserted < 30s). `node --check` clean on all 9 `nodejs/*.js`. End-to-end over stdio (server spawned with temp `MCP_SQL_DATA_ROOT`): `run_sql_query` in `listTools`; UP → `isError` falsy + JSON result; DOWN (missing file) → `isError:true` `[Unavailable] database file not found: … The SQL system is unreachable.`; SLOW → `isError:true` `[Timeout] … 0.5s timeout`; WRITE → `isError:true` `[ValidationError] … read-only …`. Python suites (`src/test_*`) re-run — still pass. No stray `data/` dirs; scratch experiment files removed.
  - Notes: One deliberate implementation difference from Python, forced by the runtime and documented in `sqlAdapter.js` + `nodejs/README.md`: `node:sqlite` is synchronous with no `interrupt()`, so the Python watchdog-thread approach can't work. The Node adapter runs each query in a child process (`sqlChild.js`) and `child.kill("SIGKILL")` on timeout, then awaits `exit` to reap it (without the reap, the killed child kept the SQLite file open and Windows `rmSync` hit `EPERM` in tests). Verified empirically first that `worker_threads` + `terminate()` does NOT stop a native-blocked `node:sqlite` call (2-min hang) — hence child process + SIGKILL. `node:sqlite` needs no flag/dependency on Node ≥ ~23. BLOB values are still imperfect on both sides (Python `bytes`→`str`, Node `Uint8Array`→JSON object) — noted, not fixed.

- [x] Fix concurrent add_note losing a note; document all cross-call state
  - Date: 2026-08-31
  - Session: CC-20260831-q4h7
  - What changed: Audited everything `src/server.py` keeps between calls: (1) the module-level `notes` list, (2) `data/notes.json`, (3) `add_note` idempotency, (4) `run_sql_query` connections (fresh + closed per call — no pool/cache), (5) the external target `.db` files, (6) `DATA_ROOT`/`SQL_DATA_ROOT` (resolved once at import), (7) the single-client/single-process assumption. The one that could give a WRONG ANSWER rather than an error: the SDK runs sync tool fns on a thread pool, so two `add_note` calls overlap; each rewrites the whole notes file, so an overlapping pair could persist a pre-other-note snapshot and `data/notes.json` would come out **missing a note** while the live process still looked right — only visible after a restart. Fix in `server.py`: added `_notes_lock = threading.Lock()`; `add_note` now does `with _notes_lock: save_notes(notes + [note]); notes.append(note)` — serialised, and file-before-memory so a failed save never leaves memory ahead of disk; `get_all_notes` takes the same lock so a read never sees a mid-mutation list. `notes_store._atomic_write` temp filename is now per-write-unique (`…tmp-<pid>-<uuid4>`) instead of `…tmp-<pid>` (all threads shared one name). New `src/test_concurrency.py`: fires 40 concurrent `add_note` via `asyncio.gather` over stdio, asserts all present in `notes://all`, then reopens the server (fresh process, same data root) and asserts all 40 still on disk. README: new "What this server assumes" section — the 7 items above, each with its two-calls-at-once and restart-mid-call behaviour, item 1 marked as the fixed risk; test list + Folders updated.
  - Verification: `python src/test_concurrency.py` → `PASS: all 40 concurrent add_note calls persisted; none lost across a restart.` exit 0. Proved the test has teeth: reverted `server.py` to the pre-fix `notes.append(note); save_notes(notes)` (no lock) and re-ran → FAIL (`add_note` calls raised `FileNotFoundError` from `os.replace` racing the shared target / lost writes); restored the fix. `python -m py_compile src/*.py` clean. Full Python suite re-run: `test_notes_store`, `test_sql_adapter`, `test_concurrency`, `test_connection` all PASS.
  - Notes: Restart-mid-call safety was already in place (atomic `os.replace` → `notes.json` is never a torn file) and is documented, not changed. Not fixed, documented instead: `add_note` has no idempotency key (a client retry after a lost reply makes a duplicate note); two *separate* server processes on one `data/` dir still last-writer-wins (out of scope per ADR-0001 — stdio = one client, one process). Node `nodejs/server.js` has the same file-before-memory ordering point but NOT the concurrency race — its MCP handlers are sync bodies on the single event-loop thread, so `notes.push` + `saveNotes` can't interleave; its `notesStore.js` `atomicWrite` still uses a `tmp-<pid>` name (latent, only bites with two processes or a future async refactor). Offered to mirror the ordering + temp-name hardening to Node on request.

- [x] Mirror the add_note ordering + temp-name hardening into the Node.js reference
  - Date: 2026-08-31
  - Session: CC-20260831-q4h7
  - What changed: `nodejs/server.js` `add_note` now does `saveNotes([...notes, note]); notes.push(note);` (file before memory) instead of `notes.push(note); saveNotes(notes);` — a failed save no longer leaves the in-memory array ahead of disk. `nodejs/notesStore.js` `atomicWrite` temp filename is now `` `${file}.tmp-${process.pid}-${randomUUID()}` `` (per-write-unique) instead of `` `${file}.tmp-${process.pid}` ``. No lock added — the Node SDK runs each tool handler's synchronous body to completion on the single event-loop thread, so two `add_note` calls cannot interleave their save + push (unlike the Python thread-pool case); a comment in `server.js` states this. `nodejs/README.md` "Where notes are stored" gained a short cross-call-state paragraph pointing at the root README's "What this server assumes".
  - Verification: ad-hoc check — 40 concurrent `add_note` via `Promise.allSettled` over stdio, then a fresh server process reads `notes://all`: `rejected=0 isError=0 missing_after_restart=0`. `node --check` clean on `server.js` + `notesStore.js`. `npm test` (all three suites: connection, notesStore, sqlAdapter) → all PASS, exit 0.
  - Notes: The concurrency race the Python fix addressed genuinely does not exist on the Node side; this change is purely the ordering + temp-name robustness, so the two implementations read the same. Still shared with Python as documented-not-fixed: no `add_note` idempotency key; two separate server processes on one `data/` = last-writer-wins.

- [x] Make add_note idempotent (retry-safe) in both server implementations
  - Date: 2026-08-31
  - Session: CC-20260831-q4h7
  - What changed: Closed the "documented-not-fixed" idempotency gap from the previous two entries — CLAUDE.md's "Idempotency & Replayability (NON-NEGOTIABLE)" requires every side-effecting service to be retry-safe. `add_note` gained an optional `idempotency_key` (string, ≤200). Dedup logic (`_existing_note` in `src/server.py`, `existingNote` in `nodejs/server.js`): with a key → match a stored note carrying that key (key identifies the operation, so first content wins even if title/content now differ); with no key → match an exact `title`+`content` pair. On a match, return `Note "…" already stored with id … (no duplicate created).` and do not write; otherwise create (storing the key if given) and write. Python runs the check inside the existing `_notes_lock`, so two simultaneous identical calls still yield one note. Stored field: `idempotency_key` (Python) / `idempotencyKey` (Node camel, like `createdAt`); public MCP arg is `idempotency_key` on both. New tests `src/test_idempotency.py` and `nodejs/test-idempotency.js` (identical retry → 1 note; same title/different content → 2 notes; reused key + different content → 1 note, first wins, same id; keyed dedup survives a restart). Wired into `nodejs/package.json` `test` + new `test:idempotency`. READMEs: main "What this server assumes" item 3 rewritten from "not idempotent" to the dedup behaviour (incl. the trade-off that two byte-identical keyless notes collapse to one); tool bullet, test lists, and Folders updated on both.
  - Verification: `python src/test_idempotency.py` → `PASS: add_note dedups on idempotency_key and on identical title+content.` exit 0. `node test-idempotency.js` → same PASS. Full Python suite (`test_notes_store`, `test_sql_adapter`, `test_concurrency`, `test_idempotency`, `test_connection`) all PASS. `npm test` (4 suites) exit 0. `python -m py_compile src/*.py` + `node --check` clean. `test_concurrency` still passes (40 unique-title notes, no false dedup). No stray `data/` dirs or scratch files.
  - Notes: Content-based fallback (no key ⇒ dedup on exact title+content) is a deliberate trade — makes the common client retry safe by default at the cost of not being able to store two byte-identical notes without distinct keys; documented in the README. The remaining documented-not-fixed item is genuinely out of scope: two *separate* server processes sharing one `data/` dir stay last-writer-wins — fixing that needs cross-process file locking or a real datastore, which ADR-0001 scopes out (stdio = one client, one process) and CLAUDE.md would treat as an architecture escalation.

- [x] Add week-06 walkthrough recording of run_sql_query end to end
  - Date: 2026-08-31
  - Session: CC-20260831-q4h7
  - What changed: User had no screen recorder, so generated one from a real run. Drove `src/server.py` over stdio (`capture_frames.py` in the session scratchpad) through a 6-step data-pipeline incident investigation (STORY-004 / REQ-004: "revenue wrong since 2026-08-25, find the data-quality root cause") against a synthetic 119k-row SQLite warehouse with an injected fault (30% NULL `amount` + ~250 duplicate `order_id` from 2026-08-25, `orders_load-2026-08-25` run marked `status='partial'`). Captured the real transcript + real per-step wait times, rendered a terminal-style animated GIF with Pillow (`render_gif.py`, `C:\Windows\Fonts\consola.ttf`). Committed artifact: `mcp-server/artifacts/week-06/data-pipeline-incident-investigation.gif` (512 KB, 800×624, 15 frames, ~25 s, loops). `mcp-server/README.md`: replaced the placeholder `.mp4` link added earlier with an inline `![…](…gif)` embed at the very top (GitHub auto-plays GIFs inline; an mp4 committed to the repo does not embed) + a two-line caption stating it is rendered frame-for-frame from a real stdio run, not a GUI screencast.
  - Verification: `capture_frames.py` completed — `captured 15 frames`; the run itself reproduced the expected outcome (Step 1 revenue −30% and null_amts jump from 2026-08-25; Step 2 pct_null 0→30.5%; Step 3 `rows=100 truncated=True`; Step 4 `orders_load-2026-08-25 status=partial rows_loaded=4200`; Step 5 `[QueryError] no such column: customer_email`; Step 6 `[Timeout] … 0.2s`). GIF re-opened with Pillow: `n_frames=15`, `size=(800, 624)`, 524,854 bytes — well under GitHub's 100 MB limit, no compression or LFS needed. Preview frames inspected visually. `Pillow 12.3.0` was `pip install`ed locally as a rendering tool only — not added to any project dependency file.
  - Notes: Delivered as GIF rather than MP4 on purpose — it plays inline on the rendered README with no player and is only 512 KB, which resolves last turn's "GitHub 100 MB / how to compress" question outright. The recording is honestly labelled in the README as generated from a real run. The 9 MB synthetic `warehouse.db` and the capture/render scripts live in the session scratchpad, not the repo; say so if a committed reproducer is wanted.

- [x] Swap the week-06 walkthrough recording from GIF to MP4
  - Date: 2026-08-31
  - Session: CC-20260831-q4h7
  - What changed: Per user request, replaced `mcp-server/artifacts/week-06/data-pipeline-incident-investigation.gif` (`git rm`) with `…/data-pipeline-incident-investigation.mp4` (H.264 High, yuv420p, 800×624, 30 fps, `+faststart`, no audio, 31.1 s, 668 KB). Rendered from the same `frames.json` (same real stdio run) via a bundled ffmpeg 7.1 from the `imageio-ffmpeg` pip package (encoder tool only, not a project dependency). `mcp-server/README.md` top changed from an inline `![…](…gif)` embed to a `▶ Walkthrough recording` link to the `.mp4`, with a note that GitHub plays it on the file's blob page (a repo-committed mp4 does not auto-embed in a rendered README, unlike a GIF).
  - Verification: `ffmpeg -i … -f null -` decoded the mp4 with no errors; `ffmpeg -i` reports `Duration 00:00:31.13`, `Video: h264 (High) … yuv420p … 800x624 … 30 fps`; extracted a frame at t=12s and eyeballed it (Step 3 `truncated=True`, Step 4 `orders_load-2026-08-25 status=partial`). Committed and pushed to `origin/mcp-server-notes-example`.
  - Notes: This drops the inline auto-play the GIF gave the README — a deliberate trade the user chose for the crisper, seekable MP4. If inline playback is wanted back, the options are: re-add the GIF, or upload the mp4 through the GitHub web editor to get a `user-attachments` URL that does embed.

- [x] Add assess_table_quality tool (MCP sampling-backed data-quality verdict)
  - Date: 2026-09-03
  - Session: CC-20260903-k9r2
  - What changed: New reasoning tool on `mcp-server/src/server.py`: `assess_table_quality(database, table)` → JSON `{verdict: PASS|WARN|FAIL, reasons[], suspect_columns[], assessed_by, statistics, sampling_ms?, model_note?}`. Split into a new pure module `src/quality_review.py` (no MCP import, mirrors the `sql_adapter.py` / `notes_store.py` extraction pattern): `gather_table_stats()` runs three read-only SELECTs through `sql_adapter.run_sql_query` (pragma_table_info for columns, one `COUNT(*)`+per-column `COUNT("c")` pass, one `COUNT - COUNT(DISTINCT)` pass over key-looking columns matched by `_KEY_NAME` = `id|*_id|*_key|*_pk|*_sk|uuid|guid|hash`) — **no model call in this step**; `heuristic_verdict()` scores those stats by fixed rules (dup key or ≥20% null ⇒ FAIL, ≥5% null or 0 rows ⇒ WARN, else PASS); `build_sampling_prompt()` builds a short system prompt + a JSON stats payload; `render()` / `render_from_model()` fold either the model reply or the heuristic into one response shape. `server.py` wrapper (async tool, takes `ctx: Context`): gathers stats itself, then `await ctx.session.create_message(messages=[SamplingMessage(...TextContent...)], system_prompt=..., max_tokens=512, temperature=0.0)` — **this is where the request leaves for the client** (server.py:296). No API key and no model name anywhere in the server — both are the client's. Degraded paths, each returns a full rule-based verdict + logs a `warning`: (a) client did not advertise `sampling` capability (`_client_supports_sampling` checks `ctx.client_capabilities.sampling`), (b) `create_message` raised (refusal / `NoBackChannelError` / transport) — caught broad, error class + message logged, `assessed_by` tagged, `sampling_ms` still reported, (c) reply present but no parseable `{verdict...}` object → heuristic verdict with the raw text kept in `model_note`. Never raises to the caller for a sampling problem; only a missing/locked DB or bad identifier raises a tagged `ToolError` (same as `run_sql_query`). `_notify()` emits an MCP log notification via `ctx.log(level, msg)` (wrapped in `warnings.catch_warnings()` — the logging + sampling capabilities are deprecated SEP-2577 but are still the client-visible channels) plus a local `logging` line, on sampling start and on return, each **with the duration in ms**; it can never raise. `quality_review.gather_table_stats` takes an optional `data_root` passthrough for in-process tests (mirrors `run_sql_query`), unused by the tool. New `src/test_quality_review.py` (stats + heuristic on clean/dup+null/empty tables, bad-identifier + unknown-table rejection, `_parse_verdict` good/garbage inputs, `render_from_model` fallback-not-crash). `src/test_connection.py` extended: asserts `assess_table_quality` in discovery; `exercise_sampling_degraded` (default client, no callback → `assessed_by` contains "does not support sampling", verdict FAIL, not an error); `exercise_sampling_ok` (client with a stub `sampling_callback` returning canned JSON → `assessed_by == "client model via MCP sampling"`, verdict follows the stub, `sampling_ms` present); now builds a temp SQLite db under a temp `MCP_SQL_DATA_ROOT`.
  - Verification: `python src/test_quality_review.py` → `ALL PASS: quality_review gathers stats, scores them, and parses replies safely.` exit 0. `python src/test_connection.py` → `PASS: connection, tool, resource, and prompt all verified.` + `PASS: assess_table_quality degrades cleanly when the client cannot sample.` + `PASS: assess_table_quality uses the client model's verdict when sampling works.` exit 0. Full regression suite re-run — `test_notes_store`, `test_sql_adapter`, `test_idempotency`, `test_concurrency` all PASS/ALL PASS, exit 0. `python -m py_compile src/server.py src/quality_review.py src/test_quality_review.py src/test_connection.py` clean. Live under MCP Inspector (`mcp dev src/server.py`) at http://127.0.0.1:6274 — both sampling and degraded paths exercised via the two test clients.
  - Notes: mcp 2.1.1 marks `sampling` and `logging` capabilities deprecated (SEP-2577) with no replacement channel; `create_message` and `ctx.log` are still the only way to do server→client sampling and log notifications, so both calls are used with a local `warnings` filter to suppress the per-call `MCPDeprecationWarning`. The broad `except Exception` around `create_message` is deliberate and requirement-driven (client refusal surfaces as several unrelated exception types) — it logs `type(exc).__name__` + message before degrading, so it is not a silent swallow. The Node.js reference (`mcp-server/nodejs/`) was NOT given this tool — offered on request; it would need the TS SDK's `server.server.createMessage` equivalent and its own degraded-path handling.

- [x] Add progress notifications to the tools that can run past ~2s
  - Date: 2026-09-03
  - Session: CC-20260903-k9r2
  - What changed: Audited the three `@mcp.tool()`s in `mcp-server/src/server.py` for >2s runtime — `add_note` (small file write, sub-second) left alone; `run_sql_query` and `assess_table_quality` instrumented. New shared helper (one place, per req 4) in `server.py`: `_progress_token(ctx)` reads `ctx.request_context.meta["progress_token"]` (returns None if the client sent no token); `_report_progress(ctx, progress, total, message)` returns without sending when there is no token (req 1 — behaves identically to before), otherwise `await ctx.report_progress(...)`, wrapped so it can never raise (req 3). `run_sql_query`: converted `def`→`async def`, added keyword-only `ctx: Context`, and moved the blocking `sql_adapter.run_sql_query` call onto `anyio.to_thread.run_sync` (the SDK used to run this sync tool on its thread pool; threading keeps the event loop — and the progress notifications — unblocked). Its work is one opaque SQL statement with no knowable duration or row count, so it emits a **rising count with `total=None`** and the messages say so ("running the query - total unknown, this can take up to Ns", "query returned N row(s)") rather than a fake percentage (req 2). 3 ticks. `assess_table_quality`: `quality_review.gather_table_stats` was split into four callable steps — `fetch_columns`, `null_counts`, `duplicate_counts`, `assemble_stats` (plus `key_column_names`); the one-shot `gather_table_stats` now just calls them in order, so its behaviour/return shape is unchanged. The tool drives the steps itself and reports **N/5 against a real total** (columns → null scan → duplicate scan → model review → verdict), 6 emissions (0..5), same 5 in every branch (sampling OK, no-sampling degrade, sampling-failed degrade). Neither tool's return value changed (req 3) — asserted byte-identical with/without progress. New tests: `src/test_connection.py` `exercise_run_sql_query` (≥3 ticks, every `total is None`, monotonic, messages contain "unknown"/"returned", body == no-progress body) and `exercise_assess_progress` (≥5 ticks, every `total == 5`, monotonic, max == 5, body == no-progress body), both wired into `main()`; helper `_Ticks` progress-callback collector. `src/test_quality_review.py` `test_step_functions_compose_to_gather_table_stats` (stepped fetch/null/dup/assemble == `gather_table_stats` for clean / dup+null / empty tables). `server.py` imports: added `anyio`, `functools`.
  - Verification: `python src/test_connection.py` → all 5 checks PASS incl. the two new progress checks (`run_sql_query emits a rising no-total progress count; result unchanged.`, `assess_table_quality emits N/5 progress against a real total; result unchanged.`). `python src/test_quality_review.py` → `ALL PASS` incl. the new compose check. Full regression suite (`test_notes_store`, `test_sql_adapter`, `test_idempotency`, `test_concurrency`) all PASS/ALL PASS, exit 0. `python -m py_compile src/server.py src/quality_review.py src/test_quality_review.py src/test_connection.py` clean.
  - Notes: `run_sql_query` moving to a worker thread is the only behavioural change beyond the notifications, and it restores parity with how the SDK executed the tool while it was synchronous (off the event loop); `sql_adapter`'s internal `threading.Timer` watchdog + `conn.interrupt()` are unaffected (already cross-thread by design). The MCP `progressToken` lives in the request `_meta`; `report_progress` is also a built-in no-op without one, so the explicit `_progress_token` check is belt-and-suspenders that also makes "did the client ask?" testable. Node.js reference not touched.

- [x] Structured log notifications across the whole MCP server
  - Date: 2026-09-03
  - Session: CC-20260903-k9r2
  - What changed: **Capability:** `mcp-server/src/server.py` now registers a `logging/setLevel` handler on the low-level server (`mcp._lowlevel_server.add_request_handler`), which flips `ServerCapabilities.logging` on — verified `get_capabilities().logging == LoggingCapability()` and `initialize().capabilities.logging is not None` over stdio. A comment states that without it the SDK silently drops every log notification (`ctx.log` returns without sending, no error). **New module `src/obs.py`:** stable event-name constants (`tool.invoked`, `external_call.started`, `external_call.finished`, `access.denied`, `error.caught`, plus `tool.completed`, `sampling.unsupported`); stable error-class names (`ValidationError`, `AccessDenied`, `TimeoutError`, `UpstreamUnavailable`, `QueryError`, `InternalError`); `new_correlation_id()` (uuid4 hex); `classify(exc)` mapping any caught exception to one of those class names (translates `sql_adapter.SqlAdapterError.error_class`, detects fs_fence/authorizer "escapes the allowed directory"/"not authorized"/"not permitted" → `AccessDenied`, `QualityInputError` → `ValidationError`, `NoBackChannelError`/`MCPError` → `UpstreamUnavailable`); `emit(ctx, level, event, correlation_id, **fields)` which sends `{event, correlation_id, **fields}` via `ctx.log` (deprecation-warning-suppressed, never raises) and mirrors it as a JSON line to the local `ai-project-mcp-server` logger; `external_call(ctx, cid, target, op, **fields)` async context manager that emits `external_call.started` then on exit `external_call.finished` (with `duration_ms`) or, on exception, `access.denied` (when `classify` == `AccessDenied`) / `error.caught` (otherwise) with a stable `error_class` + `duration_ms`, then re-raises; the yielded handle carries `.note(**fields)` (merged into the finished line) and `.duration_ms` (readable after the block, success or failure). **Instrumentation:** every tool now mints one `cid` and threads it through every line. `add_note` (→ async; `_notes_lock` `threading.Lock`→`anyio.Lock`; `save_notes` via `anyio.to_thread.run_sync`): `tool.invoked` (logs `title_len`/`content_len`/`has_idempotency_key`, never the text), `external_call` around the file write (`target=notes_file`), `tool.completed` (`note_id`, `deduplicated`). `run_sql_query`: `tool.invoked` (logs `database` — a fenced relative filename, no host/creds — plus `query_chars`/`param_count`/`timeout_seconds`/`max_rows`, never the SQL or bound params), `external_call` around the adapter call (`target=sqlite`, `call.note(row_count, truncated)`), `tool.completed`. `assess_table_quality`: `tool.invoked` (`database`, `table`), one `external_call` per SQLite pass (`fetch_columns`/`null_counts`/`duplicate_counts`) and one around the model sampling request (`target=client_model`, `call.note(model, stop_reason)`), `sampling.unsupported` on the no-sampling branch, `tool.completed` (`verdict`, `assessed_by` read back from the result body). Old free-text `_notify` helper deleted. `get_all_notes` made async (shares the `anyio.Lock`) but takes no `Context` — the SDK refuses Context injection into a static (no-template) resource, so it cannot emit MCP notifications; noted in a comment. `capture_note` made async, takes `ctx`, logs `prompt.get`/`prompt.completed` with `raw_text_len` only. **Return values unchanged** — asserted byte-identical with and without a logging opt-in; `sampling_ms` still in the assess body (now sourced from `external_call`'s `.duration_ms`). New `src/demo_trace.py` prints the ordered log lines one successful invocation produces (`assess_table_quality` → 10 lines, one correlation id). `src/test_connection.py`: new `_LogSink` + `exercise_structured_logging` — asserts the capability is advertised, every line has a non-empty shared `correlation_id`, `tool.invoked` first / `tool.completed` last, `external_call.finished` carries numeric `duration_ms`, no line contains any of a forbidden-key set (`query`, `params`, `content`, `title`, `raw_text`, `system_prompt`, `rows`, `api_key`, `connection_string`, `dsn`, …), a `../` path escape emits `access.denied`/`error_class=AccessDenied` (and still returns `is_error`), a broken query emits `error.caught`/`error_class=QueryError`.
  - Verification: `python src/test_connection.py` → 8 checks PASS incl. the three new logging ones. `python src/demo_trace.py assess_table_quality` → 10 ordered notifications, `distinct correlation ids: 1`, `tool returned (unchanged): is_error=False`. Full regression suite (`test_notes_store`, `test_sql_adapter`, `test_quality_review`, `test_idempotency`, `test_concurrency`) all PASS/ALL PASS, exit 0 — `test_concurrency` still green with `add_note` async + `anyio.Lock` (40 concurrent, none lost across restart). `python -m py_compile` clean on all 13 `src/*.py`.
  - Notes: `add_note`/`get_all_notes` moved to async because emitting an MCP log notification is an async call; `_notes_lock` is now an `anyio.Lock` held across the threaded `save_notes` (standard pattern, no deadlock — the worker never takes the lock). Static resources can't receive `Context` in this SDK, so `notes://all` has local-logger coverage only, not MCP notifications — the requirement targets tool invocations, which are fully covered. On the modern protocol MCP logging is *also* a per-request opt-in (`io.modelcontextprotocol/logLevel` in `_meta`); the client sets it via `ClientSession(log_level=...)`. The `logging` capability is deprecated (SEP-2577) but remains the only structured-log-notification channel; the one deprecation warning at handler registration is suppressed.

- [x] Enforce MCP roots containment across every filesystem-touching tool
  - Date: 2026-09-03
  - Session: CC-20260903-k9r2
  - What changed: **New module `mcp-server/src/roots_fence.py`** — one containment helper `contained_path(ctx, requested, *, correlation_id) -> Decision`: (1) asks the client for its declared roots via `ctx.session.list_roots()` (checks the `roots` client capability first; deprecation-warning suppressed), (2) `Path(requested).resolve()` to get the REAL path — collapses `.`/`..` and follows symlinks — and resolves each root the same way, (3) allows only if the real path `== root or .is_relative_to(root)` for some root (component-wise, never `str.startswith`). Module docstring explains why a raw prefix check on the unresolved path is unsound with four concrete cases (`..` walk-out, `/data-backup` vs `/data` sibling-prefix collision, a symlink under an allowed dir, case-folding). Never raises; emits its own warning log on denial — `roots.denied` (path resolved outside every declared root; fields `requested_path`, `resolved_path`, `reason`, `roots_checked`) or `roots.no_roots_declared` (client has no `roots` capability or returned an empty list → **default-deny**, `reason` ∈ `no_roots_capability`/`empty_roots_list`). Two new stable event names in `obs.py`. **`server.py`:** new thin wrapper `_require_contained(ctx, cid, requested) -> Path` calls `roots_fence.contained_path` and, on a denied `Decision`, `raise ToolError("[AccessDenied] roots containment (<reason>): <path> …")` — which in this SDK is the *return-an-error-result* path (caught one frame up → `CallToolResult(is_error=True)`, logged at info, never a traceback), not an unhandled throw; comment says so. Routed all three filesystem tools through it: `add_note` → `notes_store.notes_path()` (new export: the resolved notes.json path; hardcoded filename still goes through the gate), `run_sql_query` → `sql_adapter.SQL_DATA_ROOT / database`, `assess_table_quality` → `sql_adapter.SQL_DATA_ROOT / database` (checked once, before its 3 SQLite reads). The inner `fs_fence`/`sql_adapter`/`notes_store` per-`MCP_*_DATA_ROOT` fences are unchanged and still run (defense in depth beneath the roots gate). Not routed: `get_all_notes` (the `notes://all` resource reads the in-memory list at call time, not the file; the file read is `load_notes()` at import, pre-session; SDK also refuses `Context` injection into a static resource) and `capture_note` (no filesystem access). **Tests:** `test_connection.py` — every session now passes `list_roots_callback` declaring the temp data dirs (required for any FS tool to run); new `exercise_roots_enforced` (in-root path allowed; `../` escape → `is_error` + `[AccessDenied]` text + `roots.denied` log with `requested_path`) and `exercise_roots_default_deny` (empty roots list → `add_note`/`run_sql_query`/`assess_table_quality` all `is_error` + `roots.no_roots_declared`); the existing fs_fence `access.denied` sub-test switched to `../notes/probe.db` (inside a declared root, fails the inner SQL fence) so both layers stay covered. `test_idempotency.py`, `test_concurrency.py`, `demo_trace.py` — added a `list_roots_callback` declaring their temp root.
  - Verification: `python src/test_connection.py` → 10 checks PASS incl. the two new roots ones. `python src/test_idempotency.py`, `test_concurrency.py` (40 concurrent `add_note` with roots declared) PASS. `test_notes_store`/`test_sql_adapter`/`test_quality_review` (direct-module, no MCP) unaffected — PASS. `demo_trace.py assess_table_quality` → 10-line single-cid trace, `is_error=False`. Ad-hoc denial capture: path outside roots → `is_error=True`, `warning roots.denied {requested_path: "...sql\\..\\notes\\x.db", resolved_path: "...notes\\x.db", reason: resolved_path_outside_all_declared_roots, roots_checked: 1}`; no roots declared → `is_error=True`, `warning roots.no_roots_declared {requested_path: "...notes.json", reason: empty_roots_list, roots_checked: 0}`. `py_compile` clean on all 15 `src/*.py`.
  - Notes: `list_roots()` is one client round-trip per filesystem tool call (checked once per invocation, not per inner read) — no session cache, so a `roots/list_changed` is always honoured; the cost is accepted. `ToolError`-on-denial is deliberate and matches every other error path in this codebase (the README already documents these as `is_error: true` results); the *helper* never raises, which is the "return rather than throw" guarantee at the containment layer. Roots is deprecated (SEP-2577) but is the only client-consent mechanism for filesystem scope. Two non-tool file touches remain outside the gate by necessity: the `notes://all` resource (memory-only per call; static resources get no `Context`) and the import-time `load_notes()` (no session exists yet).

- [x] Implement the transport + state model from ADR-0001-transport.md
  - Date: 2026-09-03
  - Session: CC-20260903-k9r2
  - What changed: The user asked for `docs/TRANSPORT_DECISION.md`; that file does not exist. Asked which doc — user chose `mcp-server/docs/ADR-0001-transport.md` (STDIO default + selectable/tested Streamable HTTP behind `MCP_TRANSPORT`, per its own "Decision" text). **New `mcp-server/src/transport.py`** (the name the ADR's build plan references): `resolve()` reads the ADR's migration-outline env surface — `MCP_TRANSPORT` (stdio default | streamable-http), `MCP_HOST`/`MCP_PORT`/`MCP_MOUNT_PATH`, `MCP_JSON_RESPONSE` (default false, ADR step 2), `MCP_STATELESS` (default **false** — see notes) — into a frozen `TransportConfig`, raising `SystemExit` on a bad value. `TransportConfig.state_model` is a human string; `.notes_read_through` is True only for stateless HTTP. `run(mcp, cfg)` writes ONE flushed JSON line to **stderr** (`{"event":"server.starting","transport":...,"state_model":...,...}`) — direct `print(file=sys.stderr, flush=True)`, not the rich logger, so it is a single greppable line that survives an early SIGTERM and never touches stdout (the JSON-RPC stream on STDIO) — then calls `mcp.run("stdio")` or `mcp.run("streamable-http", host=, port=, streamable_http_path=, json_response=, stateless_http=)`. The stdio branch carries the explicit single-user comment ("do NOT scale by adding processes + a router; peak concurrency 1; the in-memory notes list is correct only because it is never shared"); the HTTP branch carries the stateless comment naming the failure it avoids (stale-replica / split-brain: a follow-up request on a different replica / a restarted process sees no session or a stale notes list) and the SDK cost (stateless = no server->client back-channel). **`server.py`:** `import transport`; `_CFG = transport.resolve()` at import; `notes = [] if _CFG.notes_read_through else load_notes()`; new `_read_notes()` returns a fresh `load_notes()` per call in stateless HTTP (no shared in-process cache — any replica serves any request) or the in-process list otherwise; `_existing_note` takes the list as a param; `add_note` dedups + writes off one `_read_notes()` snapshot and only appends to `notes` when not read-through; `get_all_notes` serves `await _read_notes()`; `__main__` is now `transport.run(mcp, _CFG)`. **`roots_fence._declared_roots`** dropped the `check_client_capability` pre-check and calls `ctx.session.list_roots()` directly (the stored capability isn't available on the stateless transport; the request is the only reliable probe) — reasons collapsed to `roots_list_unavailable` (no capability / errored) + `empty_roots_list`, both -> `roots.no_roots_declared`. **New `src/test_transport.py`:** `resolve()` mapping + rejection; STDIO spawns and logs `transport=stdio` + single-user state model; stateful HTTP (default) spawns, `add_note` with a roots callback SUCCEEDS (back-channel works), a second independent client re-inits and sees the note on disk; stateless HTTP (`MCP_STATELESS=true`) spawns, `initialize`/`list_tools` work with no back-channel, `add_note` FAIL-SAFE denies as an `is_error` result (roots/list can't run), `notes://all` still served, two independent clients connect with no shared session, startup line says stateless + names the back-channel cost. Child stderr is redirected to a file (not a PIPE) so a Windows `terminate()` can't lose the startup line. `test_concurrency.py` hardened against a benign anyio teardown ExceptionGroup that can fire after the assertions pass (40 concurrent `list_roots` round-trips make the race likelier). `obs.emit_local` (added then unused) removed.
  - Verification: `python src/test_transport.py` -> ALL PASS (resolve mapping/rejection; stdio startup line; stateful HTTP roots+add_note over the wire; stateless HTTP no-session-map + fail-safe deny + startup line). Full suite: `test_notes_store`, `test_sql_adapter`, `test_quality_review`, `test_idempotency`, `test_connection`, `test_transport` PASS; `test_concurrency` PASS (needs the teardown-noise guard; assertions clean 5+/5 runs). `py_compile` clean on all `src/*.py`. Manual: `python src/server.py </dev/null` -> one stderr line `{"event":"server.starting","transport":"stdio","state_model":"process-per-client; in-memory notes list; single user, peak concurrency 1 (ADR-0001 Decision)"}`; `MCP_TRANSPORT=streamable-http python src/server.py` -> `...,"transport":"streamable-http","state_model":"stateful HTTP; ... back-channel works ...","stateless":false,...`.
  - Notes: `MCP_STATELESS` default is **false** although the ADR frames `true` as "clean horizontal scaling": the SDK's stateless streamable-http has NO server->client back-channel even inside a request, so `roots/list` raises `NoBackChannelError` -> every filesystem tool fail-safe denies and `assess_table_quality` loses sampling. This server actively uses those stateful capabilities (roots on every FS tool, added after the ADR; sampling), which is the ADR's own "or ... if stateful capabilities are needed" branch of migration step 4 -> stateful. `MCP_STATELESS=true` still produces a genuinely stateless deployment (SDK keeps no `_server_instances` map; `notes_read_through` reads the file per request) — it just can't run the roots-gated tools. Code<->ADR disagreements were reported to the user, not silently reconciled into the ADR (requirement 5); headline ones: ADR migration step 2 says `json_response=false` lets "sampling" stream server->client (it does not under `MCP_STATELESS=true`); ADR line 147 says stateless costs "server-initiated messages outside a request" (understated — it's all server-initiated *requests*, inside a request too); ADR line 180 says diagnostics use `ctx.info`/`ctx.debug` (the code uses `obs.emit`/`ctx.log` + a stderr mirror, and the new startup line is a direct stderr write); ADR does not mention roots enforcement at all; migration steps 3/5/6/7 remain not-in-effect (auth/TLS, external datastore, health/rate-limit, full HTTP test matrix).

- [x] Add count_incident_tokens tool — reads the Anthropic token-count API
  - Date: 2026-09-03
  - Session: CC-20260903-k9r2
  - What changed: The user's prompt left the "name the system + question" placeholder blank; checked the machine (no DB running/configured, no drivers, no `*_DATABASE_URL`; `.env` has `ANTHROPIC_API_KEY` + `VOYAGE_API_KEY`; `httpx`/`anthropic` installed; `.colaberry/plan.json` `derived.systems: ["SQL"]` already served) and asked — user chose **Anthropic API — token cost**. **New `mcp-server/src/token_adapter.py`**: `count_tokens(text, *, model, timeout_s) -> int` against `POST {ANTHROPIC_BASE_URL}/v1/messages/count_tokens`. (1) `text` and `model` (model-influenced) go in the JSON **body** via `httpx.build_request(json=...)` — never concatenated into the URL/path/query; `_ENDPOINT` is a constant; `model` is regex-validated. (2) explicit `httpx.Timeout(min(timeout_s, 30))`; `httpx.TimeoutException` → `TokenAdapterError("Timeout", "token counting exceeded the {n}s timeout.")`. (3) every failure raises `TokenAdapterError` (stable `error_class` ∈ ValidationError|Timeout|Unavailable), whose `str()` is caller-safe; the wrapper converts it with `raise ToolError` → `CallToolResult(is_error=True)`, never a crash. (4) `ANTHROPIC_API_KEY` + `ANTHROPIC_BASE_URL` read from `os.environ` on every call (no host literal in source); key goes in `x-api-key`, never logged; error strings are generic ("rejected the request credentials", "could not be reached") — the `httpx.HTTPError` branch deliberately does NOT include `str(exc)` (can carry the URL); missing-config error names the vars, not values. (5) one module-level pooled `httpx.Client` (`Limits(max_keepalive=4, max=8)`), request sent `stream=True` and `response.close()` in a `finally` so the pooled connection is released even on error. (6) `server.py` wrapper `count_incident_tokens(text, model=claude-sonnet-4-5, timeout_seconds=10)`: `obs.new_correlation_id()`, `tool.invoked` (logs `text_chars` — never the text — `model`, `timeout_seconds`), `_report_progress` ticks 1/2 no-total ("counting tokens via the Anthropic API…" / "counted N input tokens"), `obs.external_call(target="anthropic_api", op="count_tokens")` for the `external_call.started`/`.finished` (duration_ms) or `error.caught` boundary logs (all carry the cid), runs the sync adapter on `anyio.to_thread.run_sync`, `tool.completed`, returns `{model, input_tokens, text_chars}`. `obs.classify` already maps `error_class=="Timeout"` → `"TimeoutError"` — no obs change. Not filesystem-touching, so no roots gate. New `src/test_token_adapter.py`: validation (empty/oversized text, bad model, bad timeout); missing `ANTHROPIC_BASE_URL`/`ANTHROPIC_API_KEY` → `Unavailable` naming the vars only; timeout fires against a local accept-but-never-answer socket (`timeout_s=0.5`, aborts <3s, `str` contains "0.5s timeout", pool stays open); pooled client reused (`_pool() is _pool()`); optional real call gated on both env vars.
  - Verification: `python src/test_token_adapter.py` → ALL PASS (real call SKIP without env). Full suite (`test_notes_store`, `test_sql_adapter`, `test_quality_review`, `test_token_adapter`, `test_idempotency`, `test_concurrency`, `test_connection`) PASS; `py_compile` clean on all `src/*.py`. **Real run** (scratchpad driver: `ANTHROPIC_API_KEY` from `.env`, `ANTHROPIC_BASE_URL=https://api.anthropic.com`, stdio, `log_level=debug`): `count_incident_tokens` on a 419-char incident write-up → `is_error: False`, `{"model":"claude-sonnet-4-5","input_tokens":134,"text_chars":419}`; log lines one cid `3aaac6a5`: `tool.invoked` → `external_call.started` → `external_call.finished {duration_ms:297.0, input_tokens:134}` → `tool.completed`. Also observed the failure contract: a mangled key earlier returned `is_error:True [Unavailable] the token-count service rejected the request credentials.` — no key/host/URL in the message, connection stayed up.
  - Notes: `ANTHROPIC_BASE_URL` has no source default (requirement 4: no host in source) — the operator sets it (`https://api.anthropic.com`); `.env` is not auto-loaded by the server, so the real run injected both vars into the child env. `stream=True` + `finally: response.close()` is what makes "release the pooled connection in a finally" literally load-bearing (a plain non-streaming `.post()` returns the connection on body-read anyway). The tool emits progress ticks though this call is typically sub-second (297ms observed) — a network call *can* exceed ~2s, which is the requirement's bar.

- [x] Fix MCP Inspector "Connect" failure — token_adapter crashed server import under `mcp dev`
  - Date: 2026-09-04
  - Session: CC-20260903-k9r2
  - What changed: The previous entry's `token_adapter.py` did `import httpx` at module top, and `server.py` imports `token_adapter` unconditionally. `mcp dev src/server.py` (what the Inspector runs) spawns the server via `uv run --with "mcp[cli]" mcp run src/server.py` — an isolated env holding only `mcp`'s dependency closure. The MCP SDK v2 depends on **`httpx2`, not plain `httpx`**, so that env has no `httpx`: `import token_adapter` → `import httpx` → `ModuleNotFoundError` → server process dies on startup → the Inspector's "Connect" fails (proxy has no live server to reach). It worked under the system Python only because that env happens to also have plain `httpx` (pulled in by the `anthropic` SDK). Diagnosed by running the exact spawn command — `uv run --with "mcp[cli]" mcp run src/server.py` — which reproduced the traceback. Fix: `token_adapter.py` no longer imports `httpx` at module load. `import httpx` moved into a new `_require_httpx()` helper (called by `_pool()` and, after argument + config validation, by `count_tokens()`); on `ImportError` it raises `TokenAdapterError("Unavailable", "the token-count tool requires the 'httpx' package (pip install httpx).")` — a clean, caller-safe error result consistent with the module's no-crash contract. Annotations were already lazy (`from __future__ import annotations`); added a `TYPE_CHECKING`-guarded `import httpx` so the module-level `_client: httpx.Client | None` hint still resolves for type checkers. Also killed a stale orphaned Inspector process tree (PIDs from a pre-fix launch) that was holding ports 6274/6275/6278 and causing a second, unrelated "PORT IS IN USE" failure on relaunch.
  - Verification: `uv run --with "mcp[cli]" mcp run src/server.py` (the Inspector's spawn, clean env with no plain `httpx`) → exit 0, no traceback (was `ModuleNotFoundError: No module named 'httpx'`). Full Connect handshake against that exact spawn: `initialize` OK, `tools/list` → `['add_note', 'run_sql_query', 'assess_table_quality', 'count_incident_tokens']`. `python src/test_token_adapter.py` → `ALL PASS` (system Python has `httpx`, full behaviour intact). `python src/test_connection.py` → all 10 PASS. `py_compile` clean. Inspector relaunched: `MCP Inspector Web is up and running at http://127.0.0.1:6274` (task b73p9jh82), no errors in its log.
  - Notes: This is the minimal robust fix — the server now always boots regardless of `httpx`, and a missing `httpx` only surfaces if `count_incident_tokens` is actually called. A fuller fix would be a `pyproject.toml`/`requirements.txt` for `mcp-server/` (it currently has none — README says just `pip install "mcp[cli]"`); adding one is a separate structural decision. As belt-and-braces an operator can also run `mcp dev src/server.py --with "mcp[cli]" --with httpx`.

## Subagent Team

- [x] Create the project-scope `explorer` subagent
  - Date: 2026-09-10
  - Session: CC-20260910-k7m2
  - What changed: Added `.claude/agents/explorer.md` at the repository root (`.claude/agents/` = project scope: version-controlled, travels with the repo, not `~/.claude`). Read-only mapping subagent — frontmatter `tools: Read, Grep, Glob` only, `model: sonnet`, and an orchestrator-facing `description` that names the trigger explicitly (a question needing more than ~5 file reads across `backend/src/services/`, `backend/src/services/agents/`, `backend/src/intelligence/`, `backend/src/routes/`, `backend/src/models/`, `frontend/src/`, `/directives`, or `mcp-server/`; maps subsystems and traces data flow — e.g. an openclaw outreach agent from directive to Express route to Sequelize model, the Cory briefing pipeline, a BuildManifest from emission to the portal state maps; never edits). Body in the required order: a ROLE fence (read-only, maps and reports, never modifies, never widens past the named subsystem), a 3-step Process (search broadly with Glob/Grep before reading, read only what matters, trace the named flow to its terminus), a No-speculation rule (unknowns go in Obstacles), and the mandated report contract — Entry points / Key modules / Data flow / Obstacles / Confidence, "return EXACTLY this structure and nothing else".
  - Verification: File written; section order and frontmatter checked against the spec. No runtime code (Markdown agent definition) so `tsc`/unit tests N/A. `.claude/agents/` confirmed not covered by `.gitignore` — `git check-ignore -v .claude/agents/explorer.md` exits 1; root `.gitignore` contains only `.env`; no global excludesfile; `.git/info/exclude` is comment-only.
  - Notes: `.claude/` is DRI-owned config (Ali Muwwakkil); this is a project-scope tooling addition on explicit user request — reversible, no runtime blast radius. `.claude/agents/` pre-existed as an empty directory; `explorer.md` is its first tracked file.

- [x] Create the project-scope `reviewer` subagent
  - Date: 2026-09-10
  - Session: CC-20260910-k7m2
  - What changed: Added `.claude/agents/reviewer.md` at the repository root (project scope, alongside `explorer.md`). Read-only risk/correctness reviewer — frontmatter `tools: Read, Grep, Glob` only, `model: opus`, and an orchestrator-facing `description`: risk and correctness reviewer, to be run before any non-trivial edit, takes a plan or a diff and returns a scored verdict (PASS / CHANGES_REQUESTED / BLOCK), read-only and never edits. Body: a ROLE fence (finds what is wrong and reports it, never fixes anything); a Scope rule (review only what the task names, never widen); four mandatory checks run every time — (1) idempotency / safe to run twice, (2) input+output contract validation, (3) failure path with a finite timeout and a bounded retry cap, (4) sensitive data logged or exposed — each mapped to the matching CLAUDE.md section; and the mandated output contract — **Verdict** (exactly one of PASS / CHANGES_REQUESTED / BLOCK), **Findings** (severity / location / problem / required fix, most severe first, `None` if empty), **Not reviewed** (out-of-scope or inaccessible items) — "return EXACTLY this structure and nothing else".
  - Verification: File written; frontmatter and the three mandated output headings checked against the spec. No runtime code (Markdown agent definition) so `tsc`/unit tests N/A. `git check-ignore -v .claude/agents/reviewer.md` exits 1 (not gitignored).
  - Notes: Second file under the previously-empty `.claude/agents/`. `.claude/` is DRI-owned config (Ali Muwwakkil); project-scope tooling addition on explicit user request, reversible, no runtime blast radius. The `explorer` question in the same user turn still carried an unfilled `[ASK SOMETHING…]` placeholder, so no explorer run was performed for it.

- [x] Create the project-scope `editor` subagent
  - Date: 2026-09-10
  - Session: CC-20260910-k7m2
  - What changed: Added `.claude/agents/editor.md` at the repository root (project scope, third file under `.claude/agents/`). Implementation stage of the explorer -> reviewer -> editor pipeline — frontmatter `tools: Read, Edit, Write, Bash`, `model: sonnet`, description: implements one scoped, already-reviewed change, to be used ONLY after `explorer` has mapped the code and `reviewer` has cleared the plan; makes the minimal edit, runs the verification gate, reports what changed. Body: a ROLE fence (implement one approved change, do not redesign/explore/widen); rules — one change touching only the named files, minimal diff (no drive-by renames/reformatting), and STOP-and-report if the task is ambiguous or the approved plan does not match the real code (no improvised alternative fix); a per-subtree verification gate (chosen by the user over a single hardcoded `tsc --noEmit`, which cannot pass — repo has no TypeScript, no tsconfig): Python under `mcp-server/src/` -> `python -m py_compile <changed .py>` + the affected `test_*.py`; `mcp-server/nodejs/` -> `npm --prefix mcp-server/nodejs test`; any subtree with a `tsconfig.json` -> `npx tsc --noEmit`; else name what was run instead. Mandated output: **Changed** (file + concrete description per line), **Verification** (exact command(s) + outcome, first error verbatim on failure, explicit pass on success), **Obstacles** (`none` when none, never blank) — "return EXACTLY this structure and nothing else".
  - Verification: File written; frontmatter and the three mandated output headings checked against the spec. Confirmed repo has no `tsconfig.json` / `typescript` dep anywhere and no `backend/` or `frontend/` source trees despite CLAUDE.md; only `mcp-server/nodejs/package.json` exists (plain `.js`, no typecheck script). No runtime code in this change (Markdown agent definition) so no gate applies to it. `git check-ignore -v .claude/agents/editor.md` exits 1 (not gitignored).
  - Notes: The generic per-subtree gate was the user's explicit choice after being shown that CLAUDE.md's mandated `tsc --noEmit` would error `No inputs were found` in this repo. `.claude/` is DRI-owned config (Ali Muwwakkil); project-scope tooling addition on explicit user request, reversible, no runtime blast radius. Subagent team now: explorer (read-only map), reviewer (read-only risk/correctness verdict), editor (scoped implement + verify).

## Deep Dives

- [x] Build Integration Engineer Field Guide (Week 7 deep dive) as a self-contained HTML knowledge base
  - Date: 2026-09-13
  - Session: CC-20260913-f3q8
  - What changed: Added `IntegrationEngineer_FieldGuide.html` at the repo root (~1,734 lines, single self-contained file) — a knowledge-base-style guide with left topic nav, search, and an offline Ask-the-Guide FAQ assistant (22 entries, no external API). Reused the exact CSS design tokens/JS diagram-and-chart engine established by `docs/SolutionArchitecture_FieldGuide.html` (SVG box/sequence-diagram renderers, Power-BI-style bar/donut/line chart renderers, search index, download/print/CSV export machinery) rather than reinventing it, and copied the mandated Colaberry logo's exact base64 payload from that sibling file via a small Node script (`docs/SolutionArchitecture_FieldGuide.html` → regex-extracted `data:image/png;base64,...` → substituted into a `__LOGO_B64__` placeholder) instead of retyping ~4,374 base64 characters by hand. Deliberately reused this accelerator's existing running example — Meridian Mutual Insurance's ClaimsPilot AI (already established across the Business Analyst, Solution Architect, QA Engineer, and other sibling field guides) — instead of inventing a new industry, since the user's prompt's own example (customer portal, legacy policy admin, payment gateway, document-OCR AI service, fraud-scoring AI service, notification service) maps directly onto that initiative's six external systems: ClaimsPilot Claimant Portal, PAS-7 (legacy Policy Administration System), Lumina Document Intelligence (OCR/LLM extraction vendor), SentinelRisk Fraud API (fraud-scoring vendor), SettleFlow Payment Gateway, and PulseNotify (notification vendor). Taught the 10-topic "20%" (REST/GraphQL/event APIs, contracts & versioning, auth, webhooks vs. polling, idempotency/retries/backoff, timeouts/circuit breakers, data mapping, rate limits, error handling/DLQ, sync vs. async), a good-vs-bad table, a KPI section, and a review-lens checklist. Built six complete documents, each a styled deliverable (branded cover, document-control strip, sign-off block, footer) with per-document "Download HTML" and "Save as PDF" buttons, plus "Export CSV" on the two tabular ones (Data Mapping, Test Plan) per the prompt's explicit scope: (1) Integration Architecture — landscape diagram, delivery-mode table, KPI tiles, latency-by-seam bar chart; (2) API Contract / Interface Spec — inbound Hub API + 5 outbound vendor contracts, JSON request/response schemas, auth table, standard error envelope, versioning policy; (3) Sequence Diagrams — happy path, async webhook+polling-fallback, an explicit retry path, and a circuit-breaker failure branch (no happy-path-only diagrams, per the prompt's quality bar); (4) Error-Handling & Retry/Idempotency Design — per-seam timeout/retry/backoff/circuit-breaker/idempotency/DLQ table, backoff-curve chart, circuit-breaker state-machine diagram, retry-outcome donut, DLQ-depth trend line chart; (5) Data Mapping Document — 22 fully-sourced fields (JS-rendered table, source of truth for both the on-page table and the CSV export); (6) Integration Test Plan — 21 cases covering happy path, 5xx, 429, timeout (including the ambiguous-outcome case), malformed payload, and duplicate-delivery/idempotency across all six seams. Embedded `<script type="application/json" id="deepdive-metadata">` with the required fields (`week: 7`, `discipline: "Integration Engineer"`, `student_id: "regina.asafor@gmail.com"`, `project_id: "IE-FieldGuide-Insurance-Deepdive"`, etc.). Opened the file in the default browser to confirm it renders.
  - Verification: `node -e "new Function(scriptBody)"` on the extracted `<script>` block parsed with no syntax errors. Automated cross-reference checks (small Node scripts, not manual inspection) confirmed: every `getElementById` container id used by the diagram/chart renderers (`dg-landscape`, `chart-backoff`, `dg-circuit`, `chart-retrymix`, `chart-dlq`, `chart-latency`, `dg-seq-happy`, `dg-seq-async`, `dg-seq-retry`, `dg-seq-failure`) exists in the HTML with zero missing; all 14 sidenav `data-nav` hrefs resolve to an actual `<section class="topic" id="...">`; the `data-download`/`data-print` button keys (`arch`, `api`, `seq`, `err`, `map`, `test`), the `data-csv` keys (`map`, `test` only, matching the prompt's "tabular documents (data mapping, test plan)" scope), the `DOC_META` object keys, and the `export-*` wrapper ids all match 1:1 with no orphans. Logo substitution verified by occurrence count (exactly 1 placeholder found and replaced) and post-substitution absence of `__LOGO_B64__` in the final file. File opened successfully via `start` in the default browser.
  - Notes: This file was found already committed to `main`-tracking branch `mcp-server-notes-example` (commit `31cfb6f`, already present on `origin` per `git fetch` + `git rev-parse` comparison) by the time this PROGRESS.md entry was written — an automatic commit/push occurred outside any explicit `git` command run in this session, bundled together with unrelated pending work (`.claude/agents/` subagent files and a `PROGRESS.md` update from an earlier, different session `CC-20260910-k7m2`) that was sitting uncommitted in the working tree beforehand. That auto-generated commit's message claimed a "PROGRESS.md" update for the field guide itself, but the actual diff only carried the pre-existing subagent-team entries — no field-guide-specific entry existed, which is the gap this entry closes (written retroactively, per the Catch-up Rule). Flagging the auto-commit behavior to the user as something to be aware of, since it bypassed this session's own commit/push controls. `student_id` follows the most recent sibling guide's convention (full email, matching `docs/QAEngineer_FieldGuide.html`) rather than the bare-username form used by earlier guides — both forms coexist across this repo's field guides, and no single canonical form has been established.

## Agentic AI System for Data Pipeline Incident Investigation

This is a separate Colaberry-portal-tracked build living alongside the rest of this
repo; its own governance is `.colaberry/CLAUDE.md` plus `.colaberry/docs/stories/STORY-nnn.md`
(read those, not this repo's root CLAUDE.md, for this project's Definition of Done —
completion is tracked in `.colaberry/progress.json`, joined against `.colaberry/plan.json`).

- [x] Sync `.colaberry/plan.json`, `progress.json`, `profile.json`, `manifest.json` to schema v2
  - Date: 2026-09-14
  - Session: CC-20260914-r7n3
  - What changed: `plan.json` was stale schema v1 while `progress.json` was already v2 (breaking the plan/progress join the Command Center relies on), and every story from STORY-001–010 in `progress.json` carried a fabricated non-null `verification` block despite no work existing — contradicting the documented invariant that `verification` is absent until a real platform verification run happens. Adopted `plan.seed.json` and `profile.seed.json` wholesale (platform-owned, no earned state in either — confirmed by comparing their sha256 against the hashes `manifest.json` already recorded as authoritative). Did NOT wholesale-replace `progress.json` (`docs/CONNECT-YOUR-REPO.md` explicitly warns against that — "it holds the ticks you have earned and the seed does not"); instead rebuilt it from `progress.seed.json`'s honest all-`false`/`null` baseline while carrying STORY-000's one genuinely earned entry forward unchanged (real verified state, commit `9db8fbc`, 73 points).
  - Verification: sha256 of the new `plan.json`/`profile.json` matches `manifest.json`'s recorded hashes exactly. Confirmed `plan.json` and `progress.json` schema_version both read 2. Confirmed STORY-000's `verification.state == "verified"` survived the rebuild and STORY-001's criteria read `[false, false, false]` post-merge (no fabricated claims left standing). Commit `e1132cb`, pushed.
  - Notes: `plan.json` excludes STORY-000 by design (10 stories, not 11) — `docs/STORIES.md` documents STORY-000 as belonging to no release and fulfilling no requirement, while `progress.json` still tracks it as one of 11 total work items; verified this is the documented shape, not a join bug.

- [x] STORY-001 — Basic Log Analysis and Recommendation
  - Date: 2026-09-14
  - Session: CC-20260914-r7n3
  - What changed: Built the `pipeline_incident_investigator` Python package end to end as a walking skeleton: `log_source.py` (`load_incident`/`load_incident_logs`, connects to the logging data source — a JSON incident record — raising typed `LogsUnavailableError`/`LogsIncompleteError` rather than swallowing failures); `pattern_matcher.py` (`match_patterns`, a small regex-driven catalog of known failure signatures — schema-mismatch, resource-exhaustion — returned ranked by evidence, empty list when nothing matches); `recommendation.py` (`generate_recommendation`, turns the strongest match into a corrective-action recommendation, returns `None` on no match, raises typed `RecommendationGenerationError` if a matched pattern has no configured action); `audit_log.py` (`record_activity`, append-only JSONL audit trail, idempotent on replay of the same `(correlation_id, activity)` pair, typed `AuditLoggingError` on write failure); `investigator.py` (`investigate`, wires all four together, audits every activity — success or failure — before re-raising). Added `data_engineering_incident_test_3.json`, a third fixture with a real but uncataloged failure (SFTP connection timeout) so the "no common pattern" acceptance criterion is demonstrated honestly rather than against a trivial clean-run log.
  - Verification: 21 unit/integration tests, all passing (`python -m unittest discover -s pipeline_incident_investigator/tests -t .`) — happy path against both real fixtures (correct category recommended in each), the negative "no known pattern" fixture (no recommendation returned), a failure path (missing log source: typed error raised, audited, then re-raised), and an idempotency replay test (same correlation_id twice does not duplicate the audit trail). `.colaberry/progress.json` STORY-001 criteria set to `true` only after these tests demonstrated each one genuinely passes.
  - Notes: Pattern catalog covers only the two signatures the provided fixtures exercise; broadening it is explicitly out of scope per the task brief ("coming next: STORY-003 SQL analysis, STORY-004 data-quality analysis" — do not build now). Confidence: 85% this story is complete and correct against its three stated acceptance criteria and the two failure paths it owns (logs unavailable, logs incomplete); would rise with a second, independent reviewer confirming the regex patterns don't over-match on a wider log corpus than the three fixtures provide, and once the platform's own verification run confirms the commit trailer was read correctly.

- [x] STORY-002 — Audit Logging for Investigations
  - Date: 2026-09-14
  - Session: CC-20260914-r7n3
  - What changed: Extended `pipeline_incident_investigator/audit_log.py` (built for STORY-001) rather than adding a parallel logging path, on the reasoning that STORY-002 hardens the existing audit trail rather than replacing it. Added SHA-256 hash-chaining: every appended entry embeds the previous entry's hash (`GENESIS_HASH` for the first line), and the new `verify_log_integrity(log_path)` walks the chain, returning the ordered entries if intact or raising a typed `AuditLogTamperedError` naming the exact line where it breaks. The on-disk record format stayed additive (`prev_hash`/`entry_hash` appended to the existing flat JSON object) specifically so STORY-001's tests kept reading it unchanged — confirmed by rerunning the full suite after the change with zero edits to the STORY-001 test files.
  - Verification: 27 unit/integration tests total (6 new, in `pipeline_incident_investigator/tests/test_audit_integrity.py`), all passing (`python -m unittest discover -s pipeline_incident_investigator/tests -t .`) — a full investigation logs and verifies every activity, a failed investigation's audit entry carries real failure details (`error_class` + message), an untampered log verifies clean, and editing, deleting, or reordering an entry are each independently caught by `verify_log_integrity`. `.colaberry/progress.json` STORY-002 criteria set to `true` only after these tests demonstrated each one genuinely passes.
  - Notes: Honest gap, documented in `audit_log.py`'s module docstring and in `.colaberry/progress.json`'s notes for this story: hash-chaining catches editing/deleting/reordering any entry, but does not catch someone truncating the *most recent* entries and re-chaining forward from there — that needs an external anchor (e.g. publishing the latest hash somewhere outside this file), out of scope for a walking skeleton. Two of the story's five listed failure paths are deliberately not addressed here rather than silently skipped: "unauthorized access to logs" is a filesystem/RBAC concern and this project has no auth system yet (REQ-018 is unfulfilled by any story); "timestamp errors" is mitigated only by using `datetime.now(timezone.utc)` (sidesteps DST-related skew) rather than any clock-sync guarantee. "Logging service unavailable" was already covered by STORY-001's `AuditLoggingError` and needed no new code.

- [x] STORY-003 — SQL Analysis for Root Cause Identification
  - Date: 2026-09-14
  - Session: CC-20260914-r7n3
  - What changed: Added `sql_source.py` (`load_incident_sql`, reads an incident's `sql_info.query` and, when present, `schema_history.current_schema` — typed `SqlSourceUnavailableError`/`SqlInfoMissingError` on failure, mirroring `log_source.py`'s shape rather than reusing its exception classes, since a junior developer reading a SQL-analysis failure shouldn't have to know it throws a class named `Logs...Error`); `sql_analyzer.py` (`analyze_query`, four independent checks — `select_star`, `missing_where_on_mutation`, `cartesian_join`, and `undefined_column`, the last comparing referenced columns against the incident's current schema when one is available — each returns `None` when it doesn't apply, so `analyze_query` returns `[]` for a clean query); `sql_investigation.py` (`investigate_sql`, orchestrates load -> analyze -> audit, deliberately built as its own entry point alongside STORY-001's `investigate()` rather than folding SQL analysis into it, since STORY-003's acceptance criteria are self-contained around issue identification and don't ask for a recommendation). Reused STORY-002's hash-chained `audit_log.record_activity` unchanged — no changes to `audit_log.py` itself.
  - Verification: 27 new tests (48 total), all passing (`python -m unittest discover -s pipeline_incident_investigator/tests -t .`). Notably, no new fixture was needed: fixture one's real query (`SELECT ... customer_type ... FROM bronze_customer_orders`) against its real `current_schema` independently rediscovers the incident's actual root cause via the `undefined_column` check alone, and fixture two's real, well-formed query is the genuine "no issues" case for the second acceptance criterion — both proven directly against the incident JSONs already in the repo, not synthetic examples. `verify_log_integrity` (from STORY-002) confirmed clean over the `sql_analysis` audit entries, satisfying the "logged for audit" criterion with the same tamper-evidence guarantee rather than a separate, weaker one.
  - Notes: `.colaberry/progress.json` STORY-003 criteria set to `true` only after the above tests demonstrated each one genuinely passes. "Performance degradation" (a listed failure path) is not addressed — there is no real SQL engine in this walking skeleton to benchmark against — noted here rather than silently skipped, same treatment as prior stories' out-of-scope failure paths.

- [x] STORY-004 — Data-Quality Analysis for Root Cause Identification
  - Date: 2026-09-14
  - Session: CC-20260914-r7n3
  - What changed: Added `data_quality_source.py` (`load_incident_data_quality`, reads an incident's `data_quality_checks` block — typed `DataQualitySourceUnavailableError`/`DataQualityInfoMissingError` on failure, same three-file shape as STORY-003's SQL analysis); `data_quality_analyzer.py` (`analyze_data_quality`, five independent checks — row-count shortfall between adjacent pipeline layers, high null rate, high duplicate rate, schema drift from unexpected/missing columns, and freshness violation — each returns `[]` when it doesn't apply); `data_quality_investigation.py` (`investigate_data_quality`, load → analyze → audit, its own entry point alongside `investigate()` and `investigate_sql()`, same pattern as STORY-003). Reused STORY-002's hash-chained `audit_log.record_activity` unchanged.
  - Verification: 26 new tests (74 total), all passing (`python -m unittest discover -s pipeline_incident_investigator/tests -t .`). No synthetic fixture needed: fixture one's real metrics independently flag both the silver-layer row collapse and the schema drift — the same root cause STORY-001/003 already identified from logs and SQL, now triangulated a third way from data-quality metrics; fixture two's real metrics flag the gold-layer shortfall, a 2.3% null rate, and 51h-vs-24h staleness, all consistent with its real root cause (resource exhaustion causing a partial, stale write); fixture three's sparse metrics (the connection-timeout incident, which never reached data extraction) correctly yield zero issues, serving as the genuine "no quality issues" case for the second acceptance criterion. `verify_log_integrity` confirmed clean over the `data_quality_analysis` audit entries.
  - Notes: `.colaberry/progress.json` STORY-004 criteria set to `true` only after the above tests demonstrated each one genuinely passes. "Performance issues" (a listed failure path) is not addressed, same reasoning as STORY-003's "performance degradation" — no real data platform exists here to benchmark against, noted rather than silently skipped. Worked one step at a time per this session's explicit paced-copilot instruction for this story: source loader confirmed and reported before the analyzer was built, analyzer confirmed before the orchestrator, orchestrator confirmed before this closeout.

- [x] STORY-010 — Coordinate Specialized Agents for Dependency Analysis
  - Date: 2026-09-14
  - Session: CC-20260914-k3f9
  - What changed: Added `agent_coordinator.py` (`coordinate_agents`), a coordination module that runs the three existing specialist agents — STORY-001's `investigate()`, STORY-003's `investigate_sql()`, STORY-004's `investigate_data_quality()` — against one incident's pipeline dependencies (REQ-002), rather than requiring each to be invoked by hand. Each agent call is wrapped catching only that agent's own already-typed exceptions, so one agent failing does not stop the others; overall `CoordinationResult.status` is `success` / `partial_failure` / `failure` depending on how many of the three succeeded. Two design forks were put to the user before writing code rather than decided silently: (1) retry lives externally — `coordinate_agents` has no internal retry loop; both its own two audit activities (`coordination_initiated`, `agent_coordination`) and each wrapped specialist's activity are already dedup'd on `(correlation_id, activity)` by STORY-002's `record_activity`, so a caller re-invoking with the same `correlation_id` after a failure is safe by construction; (2) if the final `agent_coordination` audit write itself fails, the function fails hard (raises `CoordinationAuditError`) rather than degrading gracefully, matching this project's Trust framing — but the exception carries the already-computed results on its `partial_result` attribute, so the analysis is not literally thrown away even though it isn't handed back as if it were a trustworthy, audited result. A separate `CoordinationInitiationError` covers the first audit write failing before any agent has been dispatched. An unexpected, untyped exception from an agent is deliberately left uncaught (not folded into a generic "failed" outcome) — the coordinator's honest answer to the "analysis results are incorrect" failure path: it cannot verify a specialist's correctness itself, but it will not mask a real bug behind a blanket `except Exception`.
  - Verification: 8 new tests (82 total), all passing (`python -m unittest discover -s pipeline_incident_investigator/tests -t .`). All against real fixtures, no synthetic incident data: fixture 1 (all three agents succeed → `success`), fixture 3 (SQL info genuinely missing → `partial_failure`, log and data-quality analysis still complete), a nonexistent incident path (all three fail → `failure`), a same-`correlation_id` replay on the fixture-3 partial-failure case (identical result, no duplicate audit entries — the literal "retried, recovers without data loss" criterion), both coordination audit entries present with a timestamp and matching `correlation_id` (the Trust criterion), a real OS-level failure (file-where-directory-expected) proving `CoordinationInitiationError`, a mocked summary-write failure proving `CoordinationAuditError.partial_result` retains the computed analysis, and a forced untyped `RuntimeError` from a stubbed agent proving it propagates rather than being swallowed. `.colaberry/progress.json` STORY-010 criteria set to `true` only after these tests demonstrated each one genuinely passes.
  - Notes: This is the last of the three r1 stories (STORY-003, STORY-004, STORY-010); the task brief's own "last story in the plan" framing was stale relative to `docs/STORIES.md` (which still has r2–r4 stories ahead) and was called out to the user rather than silently treated as authoritative — did not change any doc over it since it didn't affect what this story needed to build. Worked one step at a time per this session's explicit paced-copilot instruction: design forks confirmed with the user before any code was written, the coordinator module confirmed before the tests were added, tests confirmed before this closeout. Confidence: 85% this story is complete and correct against its three stated acceptance criteria and the three listed failure paths; would rise with a second reviewer confirming the fail-hard-on-audit-failure design choice is really what "Trust" should mean here (the alternative, graceful degradation, was explicitly offered to and declined by the user) and with the platform's own verification run confirming the commit trailer was read correctly.
