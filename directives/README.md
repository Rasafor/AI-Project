# directives/

One file per weekly assignment: `week-XX.md`.

Each directive states the goal, inputs, outputs, edge cases, and how success
will be verified — written *before* the corresponding `src/week-XX/` work
starts. See root `CLAUDE.md`, "Layer 1: Directives".
